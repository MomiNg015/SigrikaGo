# UI 涓婚銆佺Щ鍔ㄧ涓庝氦浜掍綋楠?

鏈枃璁板綍 Bright School 涓婚銆丆SS 鍒嗗眰銆佺Щ鍔ㄧ閫傞厤銆佸脊绐楀竷灞€銆佸弽棣堟秷鎭拰 UI 浜や簰浣撻獙銆傛柊澧炰富棰樸€佺Щ鍔ㄥ竷灞€銆佸脊绐楄瑙夊悎鍚屾垨鐢ㄦ埛鍙嶉鐣岄潰鏃朵紭鍏堟洿鏂版湰鍒嗙瘒銆?

## 褰撳墠缁撹

- 鐜╁渚х洰鍓嶅彧鏈?Bright School 涓€涓粯璁や富棰橈紝浣嗕富棰樻敞鍐屻€佷綔鐢ㄥ煙鍜?CSS 鍏ュ彛浠嶄繚鐣欐墿灞曞彛銆?
- `src/styles/base.css` 鐜板湪鏄?import-only 鍏变韩鍩虹鍏ュ彛锛屽叿浣撹鍒欐媶鍒?`src/styles/base/` 涓嬬殑 foundation銆乤sset-preload銆乻urfaces-forms-actions銆乼opbar-room-tags銆乭ome-legacy-grid銆乭ome-stage-artboard 鍜?message-feedback锛沗src/styles/room.css` 鐜板湪鏄?import-only 鎴块棿鍏ュ彛锛屽叿浣撹鍒欐媶鍒?`src/styles/room/` 涓嬬殑 layout-tabs銆乸layers-timers-skills銆乮mport-only board銆乤ctions-requests銆乸eople-floating-replay 鍜?chat-responsive锛屽叾涓?`room/board.css` 鍐嶈浆鍏?`room/board/` 涓嬬殑 frame/coordinates銆乺ow slash銆乤mbient fog銆乪ffect canvas/motion銆乸oints/preview銆乻tones/skill effects銆乴atest/touch/void 鍜?grid/scoring 鍒嗙瘒锛沗src/styles/modals.css` 鏄?import-only 鍏变韩寮圭獥鍏ュ彛锛屽叿浣撹鍒欐媶鍒?`src/styles/modals/` 涓嬬殑 base-result-skill銆乮mport-only replay-mode-resume銆乶ested-profile銆乸rofile-character-records銆乸rofile-hero-cleanup銆乸rofile-social-actions銆乧haracter-opening銆乸hone 鍜?terminal-system锛屽叾涓?`modals/replay-mode-resume.css` 鍐嶈浆鍏?`modals/replay-mode-resume/` 涓嬬殑 replay list/table銆乺esume header actions銆乵atch mode/tabs銆乺esume layout銆乤chievement modal銆乸ersonalization preview/grid銆乸ersonalization picker 鍜?resume character records 鍒嗙瘒锛沗src/styles/commerce-settings.css` 涔熸槸 import-only 棰嗗煙鍏ュ彛锛屽叿浣撹鍒欐媶鍒?`src/styles/commerce/gacha.css`銆乣social-profile.css`銆乣shop-settings.css`銆乣terminal-polish.css`銆乣warehouse-toast.css`锛沗src/styles/responsive.css` 浣滀负鍏变韩鍝嶅簲寮忓叆鍙ｏ紝鎸?900px 骞虫澘/绐勬闈€?00px 妯睆銆?60px 绱у噾甯冨眬銆?60px 绔栧睆鎴块棿銆佺煭妯睆鎴块棿鍜?620px 灏忓睆琛ㄥ崟/妫嬬洏杞叆 `src/styles/responsive/`锛沗src/styles/mobile-room.css` 浣滀负鍏变韩绉诲姩瀵瑰眬鍏ュ彛锛屾寜鍩虹 shell/dock銆佺珫灞忔埧闂淬€佽秴绐勭珫灞忋€佹í灞忔埧闂淬€佺煭妯睆鎴块棿鍜?reduced-motion 杞叆 `src/styles/mobile-room/`锛沗src/styles/hud-components.css` 浣滀负鍏变韩 HUD 鍏煎鍏ュ彛锛屾寜 HUD 纭寲銆佺獎灞?HUD 璋冩暣銆乸op-tech 缁堢椋庢牸銆佹垚鍛樻墜鍐屽彲璇绘€с€佽鑹茶繛閿佸窘绔犲拰鐢ㄦ埛韬唤瑁呴グ杞叆 `src/styles/hud-components/`锛沗src/styles/mobile-adaptive.css` 浣滀负鏈€缁堢Щ鍔ㄧ瀹夊叏灞傚叆鍙ｏ紝鎸夐€氱敤鎵嬫満銆乬acha銆乻hop銆佺ぞ浜?浠撳簱銆佷氦浜掑姩鏁堛€佹埧闂寸珫灞?妯睆銆丅right School 鍏滃簳绛夐『搴忚浆鍏?`src/styles/mobile-adaptive/`锛屽叾涓祫鏂欎簰鍔ㄦ寜閽Щ鍔ㄨ鍒欏崟鐙斁鍦?`mobile-profile-social-actions.css`锛宍bright-school-portrait.css` 鍐嶈浆鍏?`bright-school-portrait/` 涓嬬殑 resume銆乭ome plaque銆乻hop wallet銆乻ettings/tabs銆乵obile room/chat 鍜?character detail 鍒嗙瘒锛汢right School 鍩虹涓婚鍏ュ彛閫氳繃 `src/styles/themes/bright-school/base.css` 杞叆 `base/` 涓嬬殑 paper-root銆乸anels-modals銆乭ome-identity銆乭ome-gallery銆乸seudo-cleanup銆乺oom-chat-board銆乫orms-content-cards 鍜?preload-scrollbars 鍒嗙瘒锛孊right School 澶у巺鍏ュ彛閫氳繃 `src/styles/themes/bright-school/home.css` 杞叆 `home/` 涓嬬殑 canvas-purge銆乵ain-panel-material銆乸layer-zone-clips銆乻tudent-id-card銆乵anual-entry-label銆乻hort-height銆乶arrow-desktop 鍜?mobile-compact 鍒嗙瘒锛孊right School 闈欐€佸浘寤婁竴鑷存€ц鐩栭€氳繃 `src/styles/themes/bright-school/gallery-polish.css` 杞叆 `gallery-polish/` 涓嬬殑 theme-tokens銆乭ome-image-entry銆乭ome-image-art銆乸aper-surfaces銆乧hat-paper-grid銆乭ome-image-interaction 鍜?theme-addendum 鍒嗙瘒锛孊right School 鍟嗕笟瑕嗙洊鍚屾牱閫氳繃 `src/styles/themes/bright-school/commerce.css` 杞叆 `commerce/gacha.css`銆乣shop.css`銆乣warehouse-profile.css`锛孊right School 绔栧睆绉诲姩绔鐩栭€氳繃 `src/styles/themes/bright-school/mobile.css` 杞叆 `mobile/` 涓嬬殑 home銆乵odal銆乧ommerce銆乭ouse銆乴ist銆乮mport-only room銆乵otion 鍜?final-fixes 鍒嗙瘒锛屽叾涓?`mobile/room.css` 鍐嶈浆鍏?`mobile/room/` 涓嬬殑 shell-header-menu銆乿iewport-player-strips銆乥oard-stage銆乨ock-actions銆乺ecord-dialogs銆乼ouch-board-feedback銆乵odal-sheets 鍜?flat-controls 鍒嗙瘒锛汢right School 鏃╂湡鍙鎬ф竻鐞嗛€氳繃 `src/styles/themes/bright-school/contrast-purge.css` 杞叆 `contrast-purge/` 涓嬬殑 root-shell銆乻urfaces銆乧ontrols銆乫orms銆乧ards-badges銆乶otebook-details銆乵eters-friend-scroll 鍜?home-utility-tabs 鍒嗙瘒锛汢right School 鏃╂湡寮哄埗娓呭満瑕嗙洊閫氳繃 `src/styles/themes/bright-school/radical-purge.css` 杞叆 `radical-purge/` 涓嬬殑 home-top-controls銆乭ome-utility-controls銆乸rofile-handbook-cleanup銆乧haracter-detail-cleanup銆乧ommerce-social-cleanup 鍜?room-action-cleanup 鍒嗙瘒锛汢right School 楂樻潈閲嶉槻涓茶壊瑕嗙洊閫氳繃 `src/styles/themes/bright-school/specificity-overrides.css` 杞叆 `specificity-overrides/` 涓嬬殑 global-reset銆乸anel-shells銆乫orms-textareas銆乻ettings-panels銆乧haracter-details銆乥uttons銆乻crollbars 鍜?anti-tech-bleed-addendum 鍒嗙瘒锛汢right School 寮圭獥/璁剧疆/缁撴灉/鎴块棿娴眰娓呯悊閫氳繃 `src/styles/themes/bright-school/modals.css` 杞叆 `modals/` 涓嬬殑 handbook-decoration銆乻urface-cleanup銆乻ettings-lobby-cleanup銆乻elected-actions銆乺esume-personalization銆乺esult-room-popovers 鍜?stage-decoration-fixes 鍒嗙瘒锛汢right School 鎶€鑳?妫嬬洏鍔ㄦ晥瑕嗙洊閫氳繃 `src/styles/themes/bright-school/effects.css` 杞叆 `effects/` 涓嬬殑 selected-controls銆乻kill-action-active銆乻kill-action-disabled銆乥oard-targeting銆乥oard-marks銆乲eyframes 鍜?reduced-motion 鍒嗙瘒锛汢right School 瀵瑰眬鍙鎬ц鐩栭€氳繃 `src/styles/themes/bright-school/room.css` 杞叆 `room/` 涓嬬殑 header-exit銆乸layer-status銆乻kill-floating銆乸layer-name-controls銆乻ide-tags銆乥oard-coordinates 鍜?flat-controls 鍒嗙瘒锛汢right School 缁勪欢淇瑕嗙洊閫氳繃 `src/styles/themes/bright-school/component-repairs.css` 杞叆 `component-repairs/` 涓嬬殑 foundation-home銆乻hop銆乴ists-profile銆亀arehouse-character銆乺oom-board銆乧hat 鍜?notebook-polish 鍒嗙瘒锛孊right School 璐ㄩ噺鍏滃簳灞傞€氳繃 `src/styles/themes/bright-school/quality-base.css` 杞叆 `quality-base/` 涓嬬殑 audit-foundation銆乤udit-home銆乤udit-commerce銆乤udit-profile-modals銆乤udit-room銆乤udit-compact銆乺efinement-foundation銆乺efinement-controls銆乺efinement-board 鍜?sticker-motion 鍒嗙瘒锛孊right School 闃?HUD 涓茶壊鐨?firewall 鍏ュ彛閫氳繃 `src/styles/themes/bright-school/firewall.css` 杞叆 `firewall/` 涓嬬殑 root-surfaces銆乪xplicit-surfaces銆乪xplicit-pseudo-elements銆乧ontrols-forms銆乻emantic-badges銆乬eneric-surfaces銆乬eneric-pseudo-elements 鍜?typography 鍒嗙瘒銆?
- 绉诲姩绔竷灞€鐢卞叡浜Щ鍔ㄥ眰銆丅right School 绉诲姩灞傚拰鏈€缁?`mobile-adaptive.css` 瀹夊叏灞傚叡鍚岀害鏉熴€?
- 瑙嗚鍚堝悓搴旈潬 CSS contract tests 閿佸畾锛岄伩鍏嶅悗缁富棰樿鐩栨妸璇箟鐘舵€併€佸叧闂寜閽€佺Щ鍔ㄥ垪琛ㄧ瓑鎵撴暎銆?
- Board move preview and mobile touch-confirmation markers must stay centered by the `.point` grid alignment contract, because ChangLi's extra-turn placements reuse ordinary move placement hints and must stay on the same intersections as stones and SVG grid lines. Mobile touch confirmation must render only the inner `.touch-confirm-marker`; `.point.touch-confirming::before` stays hidden, and confirmation feedback may brighten the point but must not scale the whole point button.

- Startorch home terminal styles are now an import-only lobby skin entry. `src/styles/home-terminal.css` owns import order and delegates concrete rules to `src/styles/home-terminal/shell-background.css`, `top-strip.css`, `layout-player.css`, `entries.css`, `utility-footer-motion.css`, and `mobile.css`; `src/styles/styleContract.test.js` guards that directory and import map.
- Shared mobile home layout styles are now import-only as well. `src/styles/mobile-home.css` delegates the base 900px mobile/tablet rules, the 620px narrow-phone fallback, and the 900px landscape fallback to `src/styles/mobile-home/`, with `src/styles/styleContract.test.js` guarding the import order.
- Bright School portrait mobile home shell is now a nested import-only entry: `src/styles/themes/bright-school/mobile/home-shell.css` owns import order, while `src/styles/themes/bright-school/mobile/home-shell/` contains shell-base, top-strip-menu, main-stage, player-plaque, and entries-utility-footer so future mobile home, menu, plaque, and footer rules stay in focused files.
- Bright School desktop student ID card styling is now a nested import-only entry: `src/styles/themes/bright-school/home/student-id-card.css` owns import order, while `src/styles/themes/bright-school/home/student-id-card/` contains card-shell-avatar, identity-name, user-identity-tag, and mode-stats rules.
- Bright School commerce shop polish is also a nested import-only entry: `src/styles/themes/bright-school/commerce/shop.css` owns import order while `src/styles/themes/bright-school/commerce/shop/` contains sidebar-wallet, product-grid, detail-dialog, and responsive rules.
- Bright School commerce warehouse/profile polish is also a nested import-only entry: `src/styles/themes/bright-school/commerce/warehouse-profile.css` owns import order while `src/styles/themes/bright-school/commerce/warehouse-profile/` contains typography-numbers, profile-stats, warehouse-header, warehouse-empty, warehouse-item-card, and warehouse-item-mobile rules.
- Startorch HUD hardening is also a nested import-only entry: `src/styles/hud-components/hud-hardening.css` owns import order while `src/styles/hud-components/hud-hardening/` contains tokens-shell-scrollbars, inputs-settings-auth, inventory-state-tags, home-hologram-entries, character-deploy-detail, shop-pagination-owned, warehouse-surfaces, and friend-actions rules.
- Shared mobile battle shell/dock styling is also a nested import-only entry: `src/styles/mobile-room/base-shell-dock.css` owns import order while `src/styles/mobile-room/base-shell-dock/` contains shell-header-menu, flat-control-reset, viewport-dock-shell, player-timer-strip, board-viewport, dock-tabs-actions, and decision-chat-panel rules.
- Startorch terminal modal system styling is also a nested import-only entry: `src/styles/modals/terminal-system.css` owns import order while `src/styles/modals/terminal-system/` contains tokens-backdrop, modal-chrome, close-header-actions, terminal-buttons, replay-profile-surfaces, result-modal, and outcomes-resume-actions rules.
- Bright School portrait mobile house/profile styling is also a nested import-only entry: `src/styles/themes/bright-school/mobile/house-profile.css` owns import order while `src/styles/themes/bright-school/mobile/house-profile/` contains shell-profile-stats, character-grid-cards, owned-decorations, and character-detail-music rules.
- Bright School portrait mobile commerce and warehouse layout is also a nested import-only entry: `src/styles/themes/bright-school/mobile/commerce-warehouse.css` owns import order while `src/styles/themes/bright-school/mobile/commerce-warehouse/` contains shop-layout, warehouse-shell, and warehouse-items rules.
- Bright School portrait mobile list/settings/social layout is also a nested import-only entry: `src/styles/themes/bright-school/mobile/lists-settings.css` owns import order while `src/styles/themes/bright-school/mobile/lists-settings/` contains list-scroll-widths, watch-rows, friends-rows, table-shells, leaderboard-cards, replay-cards, and toolbars-card-borders rules.

## Modal Layering

- Shared `.modal-backdrop` stacks above room `--room-floating-z` surfaces so request and confirmation modals dim skill chips, chat controls, and room member popovers together.
- Mobile room portraits do not render character-chain badges; duplicate-chain data remains in user payloads but is hidden on player-facing portrait surfaces.

## Feedback Messages

- Player feedback is persisted in the `FeedbackMessage` Prisma model, backed by the `202605250001_add_feedback_message` migration.
- Logged-in users submit feedback through `POST /api/feedback`; the server stores `userId`, current `username`, normalized `content`, and `createdAt`.
- Feedback content strips control characters, trims whitespace, rejects empty submissions, and caps content at 400 characters.
- The home and room message-board modal calls the feedback API. On success it closes the modal and shows a green top toast: `鎰熻阿鎮ㄧ殑鍙嶉锛乣.
- The message-board textarea uses the same 400-character limit and displays the remaining character count below the input.
- Admins can view the latest 100 feedback messages through `GET /api/admin/feedback`.
- The admin console includes a `鐣欒█鍙嶉` tab that displays submit time, username, and full feedback content with wrapping for longer text.

## Mobile Responsive Adaptation

- `src/styles/mobile-adaptive.css` is imported at the end of `src/styles/themes.css`, preserving the existing `styles.css` theme-layer contract while making this the final 320px-768px mobile safety layer. The entry is import-only and delegates concrete rules to `src/styles/mobile-adaptive/`, while keeping existing visual themes and component markup intact and enforcing no page-level horizontal overflow, 44px minimum touch targets for buttons/tabs/icon controls, 16px mobile form inputs, safe-area padding, and media elements constrained to their containers. The portrait mobile battle-room sub-entry `src/styles/mobile-adaptive/mobile-room-portrait.css` is nested import-only and delegates shell/header/menu, neutral chrome reset, viewport shell, player strips, board viewport, dock panels, and action/decision controls to `src/styles/mobile-adaptive/mobile-room-portrait/`.
- The shared commerce sub-entry `src/styles/commerce/shop-settings.css` is import-only and delegates shop shell/tabs, settings panel, shop cards, detail/pagination, compact shop media, compact modal safety, and phone layouts to `src/styles/commerce/shop-settings/`. This keeps shop, settings, leaderboard/friends mobile guards, and warehouse/house phone fixes in named files while preserving the existing `commerce-settings.css` import position. Shop card purchase actions must keep unavailable states as native disabled controls and render sold out, already owned, not purchasable, pending purchase, and insufficient-coin buttons with one gray inactive treatment across base, Bright School, and final mobile layers.
- The shared gacha modal sub-entry `src/styles/commerce/gacha.css` is also import-only and delegates modal tabs, featured stack, machine stage, featured prize, control panel, list/result dialogs, and animations to `src/styles/commerce/gacha/`, preserving the existing commerce layer order while keeping result grid and animation rules in named files.
- The shared recruitment modal sub-entry follows the same import-only commerce style contract through `src/styles/commerce/recruitment.css` and `src/styles/commerce/recruitment/`, with mobile safety rules in `src/styles/mobile-adaptive/phone-recruitment.css`. The modal is intentionally arranged as three stable rows: header information, a bulletin-board main panel for selected/pending/ready/result states, and one-line action controls for recruitment item selection and confirmation on both desktop and mobile. The bulletin-board main panel uses `/assets/recruitment/notice-board-flat-candidate.webp`, and Bright School recruitment overrides must preserve `--recruitment-board-background-image` instead of replacing the second row with a flat color. The image uses proportional `cover` sizing with centered positioning so the second row crops from the vertical center instead of stretching the artwork. Idle, pending, and ready cards remove the foreground item icon and render the active recruitment item image as a centered CSS watermark, rotated `20deg`, softly cropped by the card bounds, and kept behind DOM text/buttons; result states do not use this item watermark. No-response and success states may layer the flat envelope and celebration sticker assets, but ready states keep only the centered response action. The compact header uses the single title `閮ㄥ憳鎷涘嫙鏍廯; pending recruitment hides the helper label and renders only the remaining time digits as CRT-style green glowing tabular text without a screen background or border; ready recruitment uses the `瞧瞧有没有新部员！` button copy, result portraits stay vertically centered, mobile item selection can drop item names and keep only icons plus quantities when the row is too narrow, selected-item confidence copy uses a red secondary cue, and unavailable recruitment confirmation must stay a gray native disabled button in base, mobile, and Bright School layers.
- The shared social/profile/leaderboard sub-entry `src/styles/commerce/social-profile.css` is also import-only and delegates modal shells, friends toolbar/search, friends list/status, friend actions/notices, duel request banner, leaderboard table, and owned decoration section rules to `src/styles/commerce/social-profile/`, preserving the existing commerce layer order while keeping social and leaderboard contracts in named files.
- General CSS architecture guard coverage lives in `src/styles/styleContract.test.js`: any CSS file with `@import` must stay import-only, and new or expanded 6000-byte CSS files fail the contract unless the known-debt baseline is intentionally updated during a split plan.
- The Startorch commerce/social polish sub-entry `src/styles/commerce/terminal-polish.css` is also import-only and delegates terminal headers/profile stats, character cards, shop shell/tabs, item cards/empty states, list/status rows, settings/warehouse target surfaces, and compact phone fixes to `src/styles/commerce/terminal-polish/`, preserving the existing commerce layer order.
- `src/styles/mobile-modals.css` is now an import-only mobile modal safety entry after `room-terminal.css` and before HUD compatibility styles. It delegates backdrop/base mobile constraints, 760px compact modal shells, 560px watch/replay/shop/leaderboard/friends/house-resume phone layouts, and reduced-motion safety to `src/styles/mobile-modals/`, while modal feature tests read the import tree so the split does not weaken the existing layout contracts.
- Bright School's final mobile guard entry `src/styles/mobile-adaptive/bright-school-overrides.css` is also import-only. Its concrete rules are split under `src/styles/mobile-adaptive/bright-school-overrides/` by home/auth header, replay dialog, profile/house records, shop cards, leaderboard cards, and deployed-character state, preserving the original 760px portrait cascade order without keeping one large late override file. The profile/house records sub-entry `src/styles/mobile-adaptive/bright-school-overrides/profile-house-records.css` is nested import-only as well, delegating house/profile stat fixes, profile resume stats, character record dialog rules, and embedded resume character records to `src/styles/mobile-adaptive/bright-school-overrides/profile-house-records/`.
- The Startorch live-room terminal skin keeps `src/styles/room-terminal.css` as the root import point after `mobile-room.css`, but concrete rules now live under `src/styles/room-terminal/` for shell/theme variables, header tags, player/timer/skill panels, board/actions, shared panels/chat/replay, portrait mobile, and landscape mobile. Room tests read the import tree so the skin contract follows the split files instead of depending on one monolithic CSS source.
- Shared room player/timer/skill styling is also nested import-only: `src/styles/room/players-timers-skills.css` delegates side layout, player cards, capture tooltips, timers, skill chips, mobile tap tooltips, and color badges to `src/styles/room/players-timers-skills/`.
- The mobile room layout remains driven by `MobileRoomLayout`, but portrait and landscape breakpoints now prioritize the board as the primary region. Portrait uses a single-column stack with compact opponent/self player strips, a board size calculated from remaining `100dvh` space, and a bounded bottom tab dock. Landscape uses a three-column board-centered grid with player strips as side rails and the dock below, so the board stays visually dominant on phones and narrow tablets. Short landscape player rails hide portrait art, rank/rating tags, capture counters, and skill chips, keeping only the player identity and timer inside the side cards instead of clipping overflow content.
- `MobileRoomLayout` is now a shell only. `RoomBattleStage` owns the rendered mobile battle grid, player strips, board slot, and bottom tab dock, so the layout no longer tries to extract composite React children before they render. The mobile action dock keeps player actions in one five-button touch row with 44px-plus targets, short visible text labels, and a bounded operation-hint region, while low-frequency members/chat panels stay behind tabs. Landscape phone layout keeps a larger board-centered grid instead of compressing the board into a miniature preview; in this mode the action tab suppresses the operation hint, uses a 44px no-padding single-row dock, and shifts the dock upward into the board gap so the five game-action buttons stay inside the short viewport.
- Modal and sheet behavior is normalized for mobile: shop, house, warehouse, leaderboard, friends, settings, message-board, profile, result, replay, and detail dialogs fit within `100dvh`, keep their outer frame inside the viewport, and assign scrolling to internal content regions. Leaderboard, friend rows, watch rows, and replay tables use mobile cards where compact reading matters; only views that truly need table comparison keep local horizontal scrolling instead of causing page overflow. The final `mobile-adaptive.css` safety layer explicitly keeps friend and watch lists on card-width rows with `overflow-x: hidden`, so later theme repairs cannot accidentally restore the old wide-table scroll behavior.
- Mobile interaction feel is owned by the final mobile CSS layers instead of JSX-specific handlers. `src/styles/mobile-adaptive.css` defines shared tap tokens, removes mobile tap highlights, gives buttons/cards/rows a short 120ms press response, keeps internal scroll containers smooth and contained, and adds lightweight backdrop/sheet entrance animations with a `prefers-reduced-motion` fallback.
- Bright School's portrait mobile layer is an import-only entry that mirrors the same interaction contract with high-specificity theme rules: home entries, utility buttons, modal controls, shop/warehouse/house cards, table rows, and room dock tabs all get touch-friendly press feedback; board points keep `touch-action: none` so taps place/confirm stones without page drag, while previewable and touch-confirming points get a subtle scale/brightness response. Board point buttons are exempt from ordinary button chrome in both shared board CSS and the Bright School refinement-board layer: they stay transparent with no appearance, border, shadow, background image, or inherited 44px min-size, so theme button rules cannot paint a white button sheet above the SVG grid. The `.board-lines` SVG grid is likewise protected from broad media resets by full-size, no max-size, visible-stroke guard rules.
- Bright School star points must render their black dot through `.point.star::after`, not `.point::before`; `.point::before` is the shared move-hover and skill-target preview layer, so desktop star-point hover must still show the ordinary move hint.
- Default black/white stone styles must not use `border` or `inset` shadows to form an outline ring; Bright School default stone overrides must stay ringless. Semantic skill states such as hidden-hand, flipped, and double-move may still use glow rings or state halos.
- Commerce and settings controls use mobile grid tabs/pagination with wrapping labels instead of squeezed inline buttons. Shop mobile layout collapses to a top summary strip plus a two-column item grid, with one-column fallback below 360px; card purchase actions retain full-width 44px touch targets.
- Bright School's final mobile layer keeps login and home titles on one line by scaling type within bounded widths instead of truncating the main title. The login brand and login/register segmented control no longer add tinted background panels on phones or desktop, so the paper grid remains the dominant surface; the outer login card keeps the same lower-right hard shadow depth as Bright School buttons.
- Bright School home image entries use the final import-only `src/styles/mobile-adaptive/home-narrow-desktop.css` safety layer even on desktop breakpoints. Concrete rules live under `src/styles/mobile-adaptive/home-narrow-desktop/` by common region reset, 1181px-1500px wide stage, 1024px-1180px compact stage, 701px-1023px micro-stage scroll, and short-height stack concerns. The card shell can keep labels and sticker shadows visible beyond the border, but the actual member manual and match artwork `<img>` nodes are forced to `box-sizing: border-box`, `height/max-height: 100%`, and `object-fit: contain`, preventing large or narrow desktop layouts from letting artwork spill below the button frame.
- Bright School 妗岄潰棣栭〉閲囩敤鍥涙鍝嶅簲寮忓绾︼細`>=1181px` 鏄?wide desktop锛屼富鑸炲彴鏃犳í鍚戞粴鍔ㄥ苟璁╁尮閰嶅叆鍙ｆ垚涓轰竴绾ц瑙変富瑙掞紝鍏朵腑 `1181px-1500px` 棰濆鍘嬬缉鍒楅棿璺濆苟缁欑帺瀹堕摥鐗屼繚鐣欏彲璇诲垪瀹斤紝閬垮厤 1280x768 绛変腑绛夋闈㈣鍒囨浣嶇Н鍒嗭紱`1024px-1180px` 鏄?compact desktop锛屼娇鐢?`player/manual/utility/match` 鍛藉悕缃戞牸涓斾笉鍏佽椤甸潰绾фí鍚戞粴鍔紱`701px-1023px` 鏄?micro desktop锛宍.home-main-panel` 鎻愪緵鍙楁帶妯悜婊氬姩锛屽唴閮?`.home-stage` 鍥哄畾 `960px` 鏈€灏忚垶鍙板搴︼紝榛樿浼樺厛闇插嚭閾墝鍜屽尮閰嶅叆鍙ｏ紱`<=700px` 缁х画璧扮Щ鍔ㄩ椤靛竷灞€銆傛牳蹇冨彲鐐瑰嚮鍏ュ彛蹇呴』鍦ㄦ甯?grid flow 鍐咃紱Bright School 鐜╁閾墝鍐呴儴淇濇寔澶村儚銆佺敤鎴峰悕銆佹浣嶉潰鏉夸笁鍒楀浐瀹氱粨鏋勶紝鐢ㄦ埛鍚嶅彧鑳藉湪涓棿鍒楀唴缂╂斁鍜屾敹鏁涳紝涓嶈兘璺ㄥ垪閬尅娈典綅闈㈡澘锛涗綆楂樺害妗岄潰鍏佽绾靛悜婊氬姩锛宖ooter 鍙湪瀹芥闈笖楂樺害鍏呰冻鏃跺浐瀹氬湪瑙嗗彛鍙充笅瑙掋€傜Щ鍔ㄧ瀵瑰紙妯″紡閫夋嫨鍗＄墖鐨勮鍒欐枃妗堝浐瀹氫负鈥滆矾鏁奥锋椂闂粹€濅笌鈥滆创鐩?瑙勫垯鈥濅袱琛岋紝璁℃暟鍗＄墖鍥哄畾璐村埌瀵瑰紙鎸夐挳鏈€鍙充晶涓斺€滃尮閰嶄腑鈥濅繚鎸佷竴琛岋紱Bright School 绔栧睆璁剧疆寮圭獥鐨勬爣棰樿楂樺拰 tabs 涓夊垪甯冨眬鐢辨渶缁?portrait 瀹夊叏灞傛帶鍒讹紝闃叉鏍囬琚鍒囨垨 tab 鎹㈣銆?
- Bright School portrait home/profile/shop fixes that must survive theme overrides live in the final import-only `src/styles/mobile-adaptive/bright-school-portrait.css` layer and its `src/styles/mobile-adaptive/bright-school-portrait/` domain files. The home player plaque reserves a non-collapsing middle column for `UserIdentity`, keeps equipped nameplates visible inside the username tag, and keeps character-chain badges hidden so they do not cover character art. The resume header uses a two-row grid on phones: title plus close button on the first row, achievement/personalization shortcuts left on the second row, and the visible coin wallet right on the second row. The final portrait rule repeats the desktop-level resume header selectors so desktop grid-column assignments cannot hide the mobile close button; its wallet slot is shrinkable and keeps the coin capsule inside the modal edge. Embedded resume character records use an internally scrolling list with taller rows instead of compressing avatars and text. Bright School shop wallet display now shows only the coin capsule while blue-gem balances remain hidden from shop and resume surfaces.
- Bright School home player plaque stats are centered inside the stats panel on desktop and mobile: the stats panel declares an inline-size container, each mode row uses three shrinkable centered columns for mode/rank/rating, and the text uses container query units so ratings stay inside the plaque when the stage or phone width changes.
- Resume character records in the final Bright School mobile layers mirror the detailed user profile character-record list: the section shell remains lightweight, and each compact row owns avatar/name plus five right-aligned stat columns for total games, wins, losses, draws, and win rate. The embedded resume list and standalone nested record dialogs keep those stats on one line with tabular numbers instead of a single combined record string.
- Resume and user profile recent-result rows share the same `.profile-rank-results` visual contract on desktop and mobile: a real `.recent-result-label` displays "鏈€杩戝崄鐩樼殑鎴樼哗" as small black text above the green/red result chips, rather than as a faint background watermark.
- Mobile user profile stat cards use centered internal grids for record, rating, and rank. The record card sizes total games and win/loss/draw as two centered lines so long results stay inside the card instead of overflowing.
- User profile footer actions keep their visual state semantic on desktop and mobile: "瀵瑰眬鍥炴斁" matches the resume replay button's green treatment, while disabled replay or relationship actions such as "宸叉槸濂藉弸" or "宸插湪榛戝悕鍗? use the shared gray inactive treatment instead of the normal action fill.
- User profile hero cards are left-aligned on desktop and mobile: the portrait stays in the first column, the username/nameplate stays in the second column, and the icon-only like/report controls are absolutely anchored to the hero card's bottom-right corner. The like button may include only the thumbs-up icon plus count, and disabled like/report states must remain native disabled gray controls across base and Bright School layers.
- Friend-list action rows and room member username popovers keep unavailable social actions as native `disabled` controls with gray inactive styling across the base, Startorch HUD, and Bright School theme layers; the not-yet-open direct-message entry stays disabled in both surfaces.
- Mobile leaderboard rows in the final Bright School layer remove extra left list inset, shrink the rank badge column, enlarge character portraits, and center the username/nameplate plus rank stack so the player rank sits directly below the username on the same vertical center axis. Compact leaderboard `UserIdentity` names also expose a fit font-size token, letting legacy/generated usernames shrink to remain readable before the name would clip. Current-user leaderboard rows, including the pinned "my ranking" row, use a light green surface on desktop and mobile to distinguish player-owned leaderboard information from ordinary entries without changing the section title treatment.
- Selected tab-like controls and selected option buttons keep one pressed-state language on desktop and mobile. Active, `aria-selected`, and `aria-pressed` controls use a shorter shadow, slight downward transform, and inset shadow so the selected state looks physically pressed into the paper surface rather than only color-highlighted.
- Settings > Interface uses the same selected-option language for its theme chooser. The page-style heading is omitted, option labels stay as direct style names without a repeated `主题：` prefix or explanatory parenthetical, and the three player-facing theme buttons stay in one three-column grid on desktop and in the final Bright School portrait mobile safety layer. The Interface tab adds a `settings-tab-theme` modal state on portrait mobile, using taller viewport-bounded rows and visible theme-panel overflow so the three choices fit without an inner vertical scrollbar. The first button is the current Bright School theme; the two future theme buttons remain clickable only to show the `敬请期待~` toast and must not mutate persisted theme state.
- Achievement and personalization modal headers use a shared two-column title/close grid on desktop and mobile. Bright School's final portrait layer repeats the static close-button placement so nested modal close controls stay on the title row instead of drifting onto the divider.
- Desktop resume modal headers use a four-column title, shortcut, wallet, and close layout. The wallet group and close button both sit in right-aligned static grid columns so currency capsules cannot overlap the close control.
- Desktop resume modal bodies use a fixed-height internal grid instead of letting the whole modal scroll; the embedded character-record section owns the vertical scrollbar, while `.profile-grid.top-stats-bar` remains overflow-visible so rating/rank help tooltips are not clipped by the stat region. Desktop user profile detail cards keep the same pattern by bounding `.profile-character-list`, while character detail headings reserve close-button space so the compact character BGM player and top-right close control do not overlap.
- Personalization equipment categories use compact style-picker entry buttons instead of listing every decoration inline. The nested picker shows default plus unlocked options, auto-closes on selection, and updates the try-on preview immediately; mobile uses a single-column internal scroll list while desktop uses an adaptive grid.
- The home footer is rendered from configurable `footerText` site settings. Desktop treats it as a viewport-fixed HUD sibling after `main.home-screen`, so it remains in the lower-right browser corner while the lobby stage scrolls; links use the safe Markdown form `[label](https://example.com)`. The mobile home footer remains part of the normal document flow rather than a fixed overlay, keeping copyright/link text below the content stack and preventing it from covering navigation cards or modal content.
- Nested replay dialogs inside the house modal use a fixed-height grid shell with the replay list as the internal scroll region on phones. Replay rows remain card-like, and long replay histories no longer depend on a bottom horizontal scrollbar or page-level scrolling.
- House replay cards on narrow phones use a bounded three-row grid with fixed 22px player portraits and single-line player names, so date/result/player/move-count content stays inside each colored row at 375px width.
- Mobile shop cards now share one compact internal contract across character, item, and decoration categories: preview, name, stock/price, and purchase button stay inside the card with the action pinned to the bottom; disabled purchase buttons remain gray and non-pressing even when Bright School portrait overrides restyle card actions.
- Key responsive breakpoints are `768px` for the general mobile adaptation layer, `768px + portrait` for phone/narrow tablet vertical layouts, `768px + landscape` for short wide devices, and `360px` for the smallest phone fallback. Existing project breakpoints at `900px`, `760px`, `620px`, and `560px` remain in place; the final mobile layer only resolves conflicts and enforces mobile usability guarantees.

## Achievement And Personalization UI

- 鎴愬氨涓庝釜鎬у寲绐楀彛娌跨敤 Bright School / 绾稿紶 scrapbook 瑙嗚锛歚house-modal` 澶栧３銆?px 鎺т欢鍦嗚銆佺焊寮犺竟妗嗐€佹煍鍜屾祬鑹茶儗鏅拰鐜版湁 modal close-button 濂戠害锛屼笉寮曞叆鏂扮殑鍝佺墝瑙嗚銆?
- 绉诲姩绔?`UserIdentity` 瑁呴グ鏈?768px 涓撶敤淇濇姢灞傦細宸茶澶囩殑寰界珷銆佺О鍙峰拰鐢ㄦ埛鍚嶈儗鏅細鍚屾缂╁皬锛岀敤鎴峰悕鑳屾櫙淇濇寔宸﹀榻愭枃瀛楋紝鎴块棿鐜╁鏉″拰鎴愬憳鍒楄〃浼氳繘涓€姝ラ檺鍒跺悕鐗屽搴︼紝閬垮厤鎵嬫満绔嚭鐜版í鍚戞孩鍑恒€?
- UserIdentity nameplates use the shared fixed-ratio badge contract across desktop and mobile: users without a nameplate keep natural-width text, equipped nameplates use a `3.75:1` background slot, and equipped username text uses shared `--user-nameplate-font-size: calc(15px * var(--user-nameplate-scale))` so text, background, and safe padding scale together. Scene CSS may change `--user-nameplate-scale`, but must not define per-surface arbitrary nameplate font sizes or stretch the tag to the parent width. Nameplate PNG assets should still be alpha-trimmed with `scripts/pngTrim.mjs` and delivered or resampled at `3.75:1`.
- 璧勬枡鍗″ご閮ㄧ户缁€氳繃 `UserIdentity` 灞曠ず宸茶澶囩殑鐢ㄦ埛鍚嶈儗鏅紝浣嗚祫鏂欏崱灞€閮ㄤ細娓呴櫎韬唤瀹瑰櫒銆佷富琛屽拰 name-tag 澶栧眰鐨勭函鑹茶儗鏉裤€佽櫄绾挎銆佹弿杈瑰拰闃村奖锛涘ご鍍忓湪璧勬枡鍗″唴涔熶笉鍔犻澶栬竟妗嗘垨闃村奖銆傜Щ鍔ㄧ璧勬枡鍗＄殑鐐硅禐/涓炬姤鎸夐挳鍙帇缂╀负鎺ヨ繎鍏抽棴鎸夐挳鍗婇珮鐨勭揣鍑戞帶浠讹紝妗岄潰绔繚鐣欏師鎸夐挳灏哄锛屼粎娌跨敤娣¤摑鐐硅禐鍜岀孩鑹蹭妇鎶ュ簳鑹诧紱涓ょ榛樿鎬佷繚鐣欐寜閽槾褰憋紝鎸変笅鎬侀€氳繃杞诲井涓嬬Щ骞跺幓鎺夐槾褰辫〃杈?pressed depth銆傝祫鏂欏崱搴曢儴鐨勫灞€鍥炴斁鎸夐挳鍦ㄦ闈㈠拰绉诲姩绔兘浣跨敤娴呯豢鍥炴斁鑹诧紝绂佺敤鍥炴斁鍜屼笉鍙敤鍏崇郴鎸夐挳缁熶竴浣跨敤鐏拌壊娓愬彉銆佸急鍖栨枃瀛椼€乶ot-allowed 鍏夋爣锛涗妇鎶ヨ緭鍏ュ脊绐椾娇鐢ㄥ浐瀹氳鍙ｅ眳涓畾浣嶏紝骞跺湪鐭鍙ｅ唴鑷繁婊氬姩锛屼笉鑳界户鎵挎埧闂存诞灞傜殑鍙充晶閿氱偣锛涙渶缁?`mobile-adaptive/profile-report-dialog.css` 瀹夊叏灞備細鐢ㄩ珮浼樺厛绾у搴﹀拰瀹氫綅瑕嗙洊 Bright School 瀵?`.room-floating-modal` 鐨勫闈㈡澘瑙勫垯銆?
- 鎴愬氨绛涢€?tab 浣跨敤涓夌瓑鍒嗘寜閽紝妗岄潰绔垪琛ㄤ繚鎸佹垚灏卞悕銆佸唴瀹瑰拰濂栧姳涓夊垪瀵归綈锛沗unachieved` 琛屼娇鐢ㄧ伆鑹茶儗鏅紝`achieved` 琛屼娇鐢ㄦ祬榛勮壊鑳屾櫙銆傝揪鎴愭椂闂撮€氳繃鐐瑰嚮宸茶揪鎴愯鏃朵粠鐐瑰嚮浣嶇疆闄勮繎寮瑰嚭鐨勫浐瀹氬畾浣嶆椂闂存诞绐楀睍绀猴紝涓嶅啀鍗犵敤鍒楄〃鍒楀锛屼篃涓嶅弬涓庢垚灏辩獥鍙ｅ唴閮ㄦ帓鐗堬紱娴獥浼氭寜妗岄潰鎴栫Щ鍔ㄨ鍙ｈ竟鐣屾敹鏉熴€?60px 浠ヤ笅闅愯棌琛ㄥご骞跺皢姣忔潯鎴愬氨鍙樻垚鍗曞垪鍗＄墖锛岄伩鍏嶅鍔卞唴瀹规尋鍘嬨€?
- 涓€у寲绐楀彛妗岄潰绔娇鐢ㄩ瑙堝尯鍔犱笁鍒楄澶囬潰鏉匡紝绉板彿銆佸窘绔犮€佺敤鎴峰悕鑳屾櫙鍒嗗埆鐙珛閫夋嫨锛涢瑙堝尯閫氳繃 `UserIdentity` 灞曠ず淇濆瓨鍓嶈瘯绌挎晥鏋溿€傝澶囨寜閽姸鎬侀伒寰垚灏?tab 鐨勭矇绾㈤€変腑瑙嗚锛氱矇绾㈣〃绀哄綋鍓嶅凡淇濆瓨骞剁敓鏁堬紝娴呯豢鑹茶〃绀烘鍦ㄨ瘯绌夸絾灏氭湭淇濆瓨銆?60px 浠ヤ笅鏀逛负鍗曞垪闈㈡澘锛屾寜閽繚鎸?42px 浠ヤ笂楂樺害鍜屽唴閮ㄧ渷鐣ュ彿锛屼繚璇佺Щ鍔ㄧ鍙偣閫夈€?
- 閮ㄥ憳鎵嬪唽鐨勫嚭鎴樼姸鎬佸垎涓ゅ眰琛ㄨ揪锛氳鑹插崱鏈綋浣跨敤娴呯豢鑹茶〃绀哄綋鍓嶅嚭鎴樿鑹诧紝鍗″唴鍑烘垬鎸夐挳澶嶇敤 tab/瑁呭鎸夐挳鐨勭矇绾㈤€変腑瑙嗚骞朵繚鐣?pressed depth锛涙闈㈠拰绉诲姩鏈€缁堝眰閮芥樉寮忚鐩栬鐘舵€侊紝閬垮厤 disabled 鎴栭€氱敤鎸夐挳鏍峰紡鎶婂綋鍓嶅嚭鎴樻寜閽礂鍥炵伆鑹层€?
- `toast.achievement` 浣跨敤閲戞鑹查珮浜笎鍙樺拰鏇村己闃村奖锛屼綔涓烘垚灏辫揪鎴愬弽棣堬紱鏅€氭垚鍔?閿欒 toast 鏍峰紡涓嶅彉銆?

## Mailbox UI And Mobile

- The home mailbox entry uses a `lucide-react` mail icon in the existing top-right action cluster on desktop and the compact mobile menu on phones. The badge is the unread plus claimable count from the summary API.
- `src/styles/modals/mailbox.css` defines the player mailbox modal as a two-column list/detail layout on desktop. Mobile switches to a top horizontal mail strip above the detail pane, with newer mail cards on the left and older cards to the right. The detail pane stretches to the bottom of the mailbox content area, while long mail body text scrolls inside `.mailbox-body` so time, title, attachment, and actions remain anchored in the detail frame.
- Empty player inboxes keep the same mailbox layout as populated inboxes: the mail strip renders a compact empty row, and the detail pane stays reserved instead of replacing the whole modal with a separate empty card.
- Player mailbox headers show only the Chinese title, with the close button kept as a static control on the same title row across desktop and in the final Bright School portrait mobile safety layer. Mail list rows use status color as a primary scan cue: pink for unread/unclaimed mail, light green for read/unclaimed mail, and gray for read/completed mail. The whole mail detail pane uses `/assets/mailbox/mail-body-paper.png` with `background-size: cover` on desktop and mobile, so the paper art fills the time/title/body/attachment/action area proportionally and may crop instead of stretching; a light translucent frosted overlay sits between the paper art and content and fades through a radial mask so the background does not compete with sent time, title, body, or attachment text while the paper remains visible. Delete is a small 24px icon-only pink circular trash button in the transparent top-right time row.
- The mailbox modal is included in the shared terminal and Bright School modal shell layers so desktop renders as a centered sheet instead of a full-width strip, while mobile keeps a top sheet with safe-area padding.
- `src/styles/admin/mailbox.css` defines the admin send form and recent send history as a two-column admin workspace on desktop and a single-column flow below 900px.
- The mailbox styles are imported through the existing import-only domain entrypoints: `src/styles/modals.css`, `src/styles/admin.css`, and `src/styles/home-terminal/top-strip.css`.

# SigrikaGo 系统设计

本文档是系统设计入口。详细设计已经按主题拆分到 `docs/system-design/`，修改架构、运行行为、资源体系、样式主题、部署方式或技术债时，应更新入口摘要或对应分篇，并运行 `npm run docs:system-design` 同步 HTML。

## 阅读顺序

1. [项目总览与功能边界](./system-design/01-project-overview.md)
2. [前端架构与状态边界](./system-design/02-frontend-architecture.md)
3. [后端、HTTP API 与实时房间](./system-design/03-backend-realtime-api.md)
4. [数据模型与领域系统](./system-design/04-data-model-and-domain.md)
5. [资源、音频与启动预加载](./system-design/05-assets-audio-preload.md)
6. [UI 主题、移动端与交互体验](./system-design/06-ui-theme-mobile.md)
7. [性能、技术债与扩展路线](./system-design/07-performance-tech-debt.md)
8. [工作流、文档规则与历史记录](./system-design/08-workflow-and-history.md)

## 当前架构摘要

- 前端使用 React 19、Vite 和 Socket.IO client；`src/main.jsx` 只负责浏览器挂载，应用组合根在 `src/app/App.jsx`，业务状态逐步下沉到 `src/app/*` hooks 和独立视图组件，应用级弹窗可见性集中在 `src/app/useOverlayState.js`，房间/回放/结果弹窗会话状态集中在 `src/app/useRoomSessionState.js`，已关闭结果不会因 `room:resume` 再弹，匹配等待/成功过渡状态集中在 `src/app/useMatchSessionState.js`；移动端对弈模式选择弹窗将路数+时间和贴目/规则分成稳定两行，设置弹窗在竖屏下保持标题不被裁切且内部 tab 单行并列；开发期 Vite `/socket.io` 代理会静默处理后端 watch 重启造成的预期 websocket 断连错误。
- Mobile root back handling is centralized in `src/app/modalDismissal.js`: login, preload, home, admin, and battle parent screens show the `确定要退出游戏吗？` confirmation when no functional modal is open; close/cancel buttons and phone back inside functional modals only dismiss the current modal and do not cascade into exit confirmation.
- 后端使用 Express、Socket.IO、Prisma 和 SQLite；`server/index.js` 负责 HTTP/Socket 入口组合，启动数据与 schema 初始化收口在 `server/serverStartup.js`，Socket 连接事件套件由 `server/socketEvents.js` 统一装配，速率保护与匹配、房间连接/恢复、对局/数子/求和/计分、聊天、约战、断线清理等行为继续分布在对应 `server/socket*Events.js` 模块，生产静态托管收口在 `server/staticAssets.js`，认证、商城、抽卡、社交、回放、房间生命周期等已拆为领域模块；匹配或约战创建的房间先进入 `preloading` 阶段，由 `room:preload-ready` 形成双方资源就绪屏障，双方都 ready 后才进入 opening/自动猜先，任一方超过 90 秒未 ready 时服务端发出 `match:preload-timeout` 并中止匹配。
- 后台管理新增面向云端运营的分析入口：`概况` 默认呈现“今日简报”，用 `正常 / 需要关注 / 需要处理` 总状态、原因、分级解读和下一步动作替代密集报表；`运营分析` 展示最近日期范围的活跃、注册、对局、玩家分层、经济和模式趋势。后端聚合逻辑集中在 `server/adminAnalytics.js`，通过 `/api/admin/analytics/overview` 与 `/api/admin/analytics/operations` 输出可读摘要，实时在线与匹配/房间统计从 `onlineSessions`、`rooms.js` 运行时边界注入，深度事件暂以 `待接入` 标注。
- 对局模式由 `src/shared/gameModes.js` 统一配置，前后端共享模式顺序、棋盘大小、贴目、技能开关和时间控制。
- Capture-removal counters may be negative and still participate in scoring: Lynae `spray-stone` and QiuYuan `row-slash` subtract 1 removal from the acting player when the skill succeeds, while ChangLi `double-move` adds 1 removal after a completed second placement.
- 当前内置对局模式为 `spark`、`standard` 和 `gomoku`。五子棋沿用 13 路棋盘与星位、5 分钟 30 秒 3 次读秒、自动猜先和独立 `UserModeStats(mode=gomoku)` 段位/积分；需要出战角色但 `skillEnabled=false`，房间 UI 不显示弃手、数子、提子、除子、超频或技能信息。 Gomoku five-in-row results carry `winner.winningLine`; the board keeps a persistent gold highlight on those five stones, live games show the result modal immediately, and replay only shows the highlight.
- 站点公开配置通过 `SiteSetting` key/value 存储，并由 `src/shared/siteSettings.js` 定义前后端共享默认值；后台系统设置可编辑大厅标题/副标题、关于文本、首页 footer 长文本、加载页提示语集合和全局技能特效开关，footer 支持受限 Markdown 链接，加载页提示语按行维护并在玩家预加载时随机轮播；加载屏内容在桌面、移动端和短视口中保持整屏居中，标题与提示语允许按容器宽度换行，中间加载面板保持透明且不覆盖整页主题背景。
- `ratingRules` controls dynamic rated-match rewards, rank-gap scaling, optional anti-boosting, rank-change rating deltas, and friendly-match coin limits. Matchmaking is rated; direct/private duels are friendly, excluded from public stats/rank windows, kept in replay history, and marked in replay UI.
- 对局规则支持黑白棋子与命名中立棋子并存；中立棋子按同名阵营连接、阻断领地并参与气/提子判定，但不归属黑白除子。`src/shared/game.js` 保留共享对局 API 门面，阶段常量、动作结果、普通落子/隐藏手动作、主动技能动作和技能状态变更 helper 已拆到 `gamePhases.js`、`gameActionResult.js`、`gameStoneActions.js`、`gameSkillActions.js` 和 `gameSkillState.js`，降低新增技能继续膨胀核心状态机的风险。
- 星炬技能体系包含面向整行的 `row-slash` 主动技能、面向空交叉点的 `protocol-takeover` 主动技能和长离（`changli`）`double-move` 连落技能；仇远（`qiuyuan`）通过“一斩足矣”指定任意有效交叉点，移除该横线所有棋子并按直接移除数增加超频，莫宁（`mornye`）通过“协议接管”把一个空点写入对手禁入协议，管理员在部员招募系统实装前默认拥有这两个部员招募角色；仇远 `row-slash` 在技能横幅结束后通过 `BoardSkillEffects` 播放不依赖外部纹理的 Pixi Graphics 青白水墨刀光：两道竖向贯穿预兆刀光（第一道自上向下、第二道自下向上扫过，角度在 -30 到 30 度间确定性随机，并留存到主刀光收势时一起淡出）和 1.8 格高主刀都去掉浅蓝透明厚波纹，改为白色直刃叠加青白渐变边光、深青墨/灰蓝墨短段不规则水墨涂抹边缘，随后从左到右刷过目标行并同步触发 `row-slash-cut-pending` 棋子消失；`BoardRowSlashOverlay` 让最终 `.board-row-slash` 以超出棋盘两侧的长度随 Pixi 主刀同一时间线从左向右裁切展开，casting 时序由 `boardEffectDurationMs * 0.19` 延迟和 `* 0.22` 持续时间驱动，刀痕起点略向下偏移以让倾斜刀痕中点交合目标行中线，结算后的同类刀痕继续来自 `game.rowEffects`，并通过 `clearAfterColor` 在对手下一次行动后清除；莫宁 `protocol-takeover` 在技能横幅后通过 `BoardSkillEffects` 播放淡紫与冰蓝数据流光束从上方向目标点落下的 Pixi 演出，并同步播放更明显的两段式数据束启动与目标锁定 SFX，结算后禁入点继续显示淡紫与冰蓝 DOM/CSS 协议标记；长离 `double-move` 在技能横幅后通过 `BoardSkillEffects` 播放透明 SVG 红色火焰凤凰飞入与不规则边界的全棋盘火焰 Pixi 演出，随后连续落下的棋子写入 `double-move-stone` 并继续显示复用透明火焰 sprite 的 DOM 持续光效；琳奈 `spray-stone` 生成的喷涂中立棋子使用 `/assets/stones/spray-stone.webp` 透明 WebP 作为 `.spray .stone` 背景，资源归入技能效果预加载组；娜波摩 `color-illusion-passive` 在被动持续期间由 `BoardAmbientEffects` 播放 pointer-transparent 的中心向外 CSS 灰白化扩散波纹，并由 `.board-wrap.color-illusion-board-surface::before` 从棋盘中心扩散切换到 `/assets/boards/nabomo-color-illusion-board.webp` 灰白木纹背景，该背景保持到被动结束，避免使用持续 Pixi 烟雾或被边界裁切的雾层；该被动通过技能演出配置层声明 2 秒横幅结束即结算，避免横幅后空档；`src/shared/skillPresentation.js` 统一解析技能演出时间线、棋盘/Pixi/DOM 图层能力和全局关闭特效开关，共享棋盘 CSS 通过 import-only `src/styles/room/board.css` 分派到 `src/styles/room/board/`，棋盘点位按钮保持透明无按钮皮肤，`.board-lines` SVG 网格层也显式保持铺满棋盘和可见 stroke，避免全局/主题按钮或媒体规则盖住、压塌棋盘网格。
- 西格莉卡 `erase-point` 结算后的无效交叉点使用 `/assets/effects/sigrika-erased-field-marker.webp` 透明 WebP 作为 point-local 坑洞标记，`.void` 以 1.5 个棋盘格子长度显示，pending 陨石命中揭示和桌面/移动端棋盘共用同一尺寸合同。
- Lynae `spray-stone` pending previews include all transformed points in `affectedPointIds` so the Pixi cast covers the selected target and resolved random target together; `Board` masks those original stones with `.spray-transform-pending` during the post-banner effect window, then `.spray-transform-pending .stone` keeps the original stone visible while an internal borderless cyan/purple/pink/yellow irregular spray-paint cover blooms above it, and switches to the same `/assets/stones/spray-stone.webp` artwork only after that cover has stayed opaque through most of the post-banner effect window, and `.spray .stone` keeps that WebP-only neutral spray stone with no CSS/SVG fallback stone, plus a point-bound colorful underside glow. During the pending paint-cover frame, `Board` derives the stone offset from the final `spray` stone type so the covered original stone and resolved neutral spray stone do not jump between positions.
- Denia/Danya `flip-stone` keeps the Pixi bubble renderer on the normal post-banner board-effect layer, but its shared pending resolution delay is `3040ms` instead of the default `4000ms`: the authoritative flipped stone snapshot lands while the bubble is already dark and covering the target, before the pop/shard phase reveals the changed stone.
- Local skill-animation GIF export is a development-only preview workflow, not a production asset pipeline. `npm run export:skill-gifs -- --effect <effect> [--character <id>]` runs `scripts/export-skill-gifs.mjs`, starts a local Vite harness under `.trellis/tasks/06-23-gif/capture-harness`, mounts the same `BoardSkillEffects`/`Board` presentation paths used in live rooms, captures frames with Playwright, and encodes looping GIFs with ffmpeg under `outputs/skill-gifs/<character-or-effect>/`. The exporter supports effect-only previews on the current `.board-wrap` board texture and full board-context previews, keeps audio muted, and uses representative mock board metadata instead of entering a real Socket.IO room. Captures run under Playwright virtual time: the harness preloads the real effect path before the cast starts, then advances the browser clock by exactly one frame per screenshot so slow screenshot I/O cannot compress a multi-second animation into a faster GIF. Board-context Voyage Star captures also switch their mock `Board` to the resolved crater/removal state during the full-white cover window, matching the live room's hidden-under-cover settlement path.
- 玩家资源包含角色、装饰、道具、音乐、抽卡奖励、蓝宝石和金币；结构化关系表与旧字符串字段仍处在兼容迁移期，legacy 解析、结构化同步和公开资产合并规则集中在 `server/userAssets.js`。
- Warehouse inventory uses single-row item entries on desktop and compact single-column row cards on mobile. Mobile warehouse item rows place the quantity chip above the use button, right-aligned in the action column. Disabled warehouse use actions such as recruitment-only items render as gray native disabled buttons on desktop and mobile. Character-target item selection disables unavailable character cards with gray native disabled state only, without showing reason badges or reason text in the card/title.
- Shop card purchase actions use native disabled buttons for every unavailable state, including sold out, already owned, not purchasable, pending purchase, and insufficient coins. Base shop CSS, Bright School shop polish, and final mobile overrides all render those disabled purchase buttons with the same gray inactive treatment instead of category-specific owned/sold-out colors.
- Player-facing character acquisition now uses the recruitment system instead of the gacha modal. Recruitment consumes warehouse items from the shop misc category, allows only one active task per user, decides the result at start time, exposes a five-minute pending period, shows the home recruitment entry pink ready background only for ready-to-view responses with a client-side readyAt refresh in addition to polling, grants a fixed-code-pool character on success, refunds the consumed item on no-response, computes remaining candidates from the merged legacy-plus-structured user asset projection, and rejects start immediately without consuming the item when the selected item has no remaining recruitable candidates. Non-production recruitment builds expose `/api/recruitment/fast-forward` and a clock icon button beside the countdown to shorten the pending task to five seconds; production rejects this route and the production UI hides the test shortcut. The recruitment modal main panel uses the flat Bright School-compatible `/assets/recruitment/notice-board-flat-candidate.webp` bulletin-board background for idle, pending, ready, and result states; the image is scaled proportionally with `background-size: cover` and vertically centered rather than stretched. State cards use the cropped `/assets/recruitment/recruitment-letter-paper-flat.webp` paper background derived from the stationery sheet; idle, pending, and ready cards remove the foreground recruitment-item icon and instead show the active item image as a centered, 20-degree rotated, softly cropped watermark behind the text/control layer. The selected-item confidence line is a red secondary cue, pending recruitment hides the helper label and renders only the remaining time digits as CRT-style green glowing tabular text without a screen background or border; Bright School desktop and mobile overrides explicitly keep the pending panel, its content wrapper, and the countdown row transparent so generic paper-surface guards cannot reintroduce a white card behind the timer. Ready recruitment keeps only the centered `瞧瞧有没有新部员！` action, mobile item buttons may collapse to icon plus quantity only, and unavailable recruitment confirmation renders as a gray native disabled button. No-response states can layer the cropped `/assets/recruitment/recruitment-envelope-flat.webp` sticker, success can layer `/assets/recruitment/celebration-flat-candidate.webp`, opening recruitment plays `/assets/music/recruitment-open.ogg`, and claimed recruitment results play one SFX through the existing `sfx` channel: `/assets/music/recruitment-success.ogg` on success or `/assets/music/recruitment-miss.ogg` on no-response.
- 玩家 UI 当前默认 Bright School 主题；主题注册、CSS 入口和作用域合同保留未来扩展口，共享基础层已从单一 `base.css` 入口拆到 `src/styles/base/`，后台管理样式已从单一 `admin.css` 入口拆到 `src/styles/admin/`，共享大厅/小屋/观战列表样式已从单一 `lobby.css` 入口拆到 `src/styles/lobby/`，房间样式已从单一 `room.css` 入口拆到 `src/styles/room/`，其中共享棋盘子入口继续拆到 `src/styles/room/board/`；Startorch 对局终端皮肤已从单一 `room-terminal.css` 入口拆到 `src/styles/room-terminal/`，共享弹窗样式已从单一 `modals.css` 入口拆到 `src/styles/modals/`，其中资料卡点赞/举报动作样式独立在 `profile-character-records.css`，点赞/举报动作样式独立在 `profile-social-actions.css`，replay/mode/resume/achievement/personalization 子入口继续拆到 `src/styles/modals/replay-mode-resume/`；移动弹窗安全层已从单一 `mobile-modals.css` 入口拆到 `src/styles/mobile-modals/`，商业/社交/仓库样式已从单一 `commerce-settings.css` 入口拆到 `src/styles/commerce/` 领域分篇，其中商店/设置/移动商业补丁继续从 `src/styles/commerce/shop-settings.css` 拆到 `src/styles/commerce/shop-settings/`，Startorch 商业/社交终端润色继续从 `src/styles/commerce/terminal-polish.css` 拆到 `src/styles/commerce/terminal-polish/`；共享响应式层已从单一 `responsive.css` 入口拆到 `src/styles/responsive/`，共享移动对局层已从单一 `mobile-room.css` 入口拆到 `src/styles/mobile-room/`，共享 HUD 兼容层已从单一 `hud-components.css` 入口拆到 `src/styles/hud-components/`，最终移动端安全层也从单一 `mobile-adaptive.css` 入口拆到 `src/styles/mobile-adaptive/`，其中 Bright School 最终移动兜底覆盖继续拆到 `src/styles/mobile-adaptive/bright-school-overrides/`，Bright School 竖屏手机最终兜底覆盖继续拆到 `src/styles/mobile-adaptive/bright-school-portrait/`；Bright School 对应早期可读性清理、早期强制清场覆盖、高权重防串色覆盖、静态图廊一致性覆盖、商业覆盖、弹窗/设置/结果/房间浮层清理、动画/技能/棋盘效果、对局可读性覆盖、移动端覆盖、竖屏对局覆盖、组件修复覆盖、质量兜底层和防 HUD 串色 firewall 分别拆到 `src/styles/themes/bright-school/contrast-purge/`、`src/styles/themes/bright-school/radical-purge/`、`src/styles/themes/bright-school/specificity-overrides/`、`src/styles/themes/bright-school/gallery-polish/`、`src/styles/themes/bright-school/commerce/`、`src/styles/themes/bright-school/modals/`、`src/styles/themes/bright-school/effects/`、`src/styles/themes/bright-school/room/`、`src/styles/themes/bright-school/mobile/`、`src/styles/themes/bright-school/mobile/room/`、`src/styles/themes/bright-school/component-repairs/`、`src/styles/themes/bright-school/quality-base/`、`src/styles/themes/bright-school/firewall/`。
- 匹配成功后先保留 3 秒“匹配成功”倒计时弹窗；倒计时结束后才进入对局资源预加载页，该页复用登录加载页视觉，只显示本机玩家出战角色的跳动立绘和角色加载台词，并显示 `资源加载中 0/2` 到 `2/2` 的服务端 ready 人数，不再显示转圈动画。前端只有在匹配成功倒计时完成且房间已离开 `preloading`（双方资源 ready）后才进入对局页；若倒计时先结束则进入并停留在资源加载页，若资源先完成则继续显示倒计时并在倒计时结束后直接进入对局页。刷新或重连恢复到仍处于 `preloading` 的玩家房间时，客户端会重新构造已完成倒计时的 pending match 并进入 `match-preloading`，重新加载本地对局资源并发送 `room:preload-ready`，而不是把该快照直接写入 active room。启动预加载区分关键资源与延迟资源，并为单个资源加载设置超时兜底，避免服务器重启或缓存恢复时把玩家卡在资源准备页；房间运行期使用轻量 `room:clock`、`room:patch` 与完整 `room:update` 分流，降低高频交互卡顿；`room:clock` 与 `room:update` 共用 `clockSeq` 时钟序号，前端会忽略比当前房间快照更旧的轻量时钟，避免乱序到达导致棋钟倒转或读秒次数回退。后端房间恢复与活跃房间判定通过用户到房间的内存索引优先定位，避免 `room:resume` 在房间增多时全量扫描；聊天与在线状态 patch 使用节流持久化，完整房间更新和关键生命周期仍强制持久化。移动端弱网下，启动预加载不订阅瞬时 Socket.IO 实例变化，对局 socket 连接使用明确重连和 6 秒握手超时，避免连接抖动把玩家反复留在资源准备页。

- Bright School desktop home keeps explicit wide desktop, compact desktop, micro desktop, and mobile portrait layout contracts. In the `1181px-1500px` desktop band, the mode stats panel must stay compact and must not cover or visually consume long usernames; the username column owns the remaining width, scales earlier, and clips inside its own tag only as a last resort. Wide desktop keeps the stage top spacing compact and shows the 3x2 utility toolbox inside the stage; match-mode counts stay in the click-open mode picker instead of rendering tickets under the match entry. Micro desktop keeps a controlled `960px` horizontal stage with core entries in normal grid flow and low-height desktop windows may scroll vertically. Phone portrait renders the home stage as a single-column usable flow instead of showing an orientation guard.
- Bright School home utility buttons use paper-card microinteractions in the final toolbox/theme layers: hover/focus lifts the left utility toolbox, active press compresses the hard shadow and moves down slightly, disabled entries keep native gray inactive styling, recruitment-ready keeps its pink alert background, and `prefers-reduced-motion` removes the transform while preserving clear state colors. Each utility entry owns only an icon and title; the phone portrait toolbox keeps the same icon-plus-title contract in a 2x3 compact grid.

## 维护约定

- 入口文档只写总览、导航和跨主题规则；详细事实写入对应分篇。
- 分篇标题、说明和新增内容优先使用中文；代码标识、路径、事件名、模型名保留原文。
- 发现乱码时先确认源文件是否含 `Unicode replacement character` 或常见 mojibake 片段，再区分终端显示问题和文件内容损坏。
- 修改 Markdown 后运行 `npm run docs:system-design`；涉及脚本或编码规则时运行 `npm test -- docs/systemDesignHtml.test.js`。

## Admin Deployment Defaults

- Non-user admin configuration defaults are captured in `server/adminDefaultSnapshot.js`, generated from the local `prisma/dev.db` admin state. Startup runs `seedAdminDefaultConfig()` from `server/adminDefaultSeed.js` after schema guards and before built-in seeders, syncing site settings, characters/skills, decorations, shop items, gacha pools/prizes, achievement rewards/achievements, music display names, and recruitment-related admin data to that deployment snapshot. Users, user assets, purchases, feedback, reports, audit logs, analytics, mailbox history, game records, and live-room state remain excluded.

## Board Effect Theme Guard

- Mobile match-mode picker cards keep rule text split into two stable lines, pin the count/status chip to the far right edge of each mode button, and keep the status label on one line.
- Room chat and presence changes now use lightweight `room:patch` paths with `eventId`, `baseRevision`, `revision`, and client-side gap recovery through `room:resume`; patch handlers skip missing-room, wrong-room, unknown, stale, and malformed payloads before scheduling React state work; `chat:append` appends messages idempotently, while `presence:update` updates player connection flags, spectators, and system chat without replacing `game`, structurally shares unchanged player/spectator/chat entries, and recovered live `room:resume` plus pending match-success snapshots reuse the same full-snapshot structural sharing path as `room:update`; duplicate pending match-success snapshots and unchanged pending-match clocks are consumed without scheduling fresh match-transition state. Board handlers refresh through a board-shell rerender when needed, stay stable across clock churn through `useRoomPointActions` and stable `RoomBattleStage` callbacks, stale `room:clock` payloads are ignored before scheduling React state writes, duplicate `lobby:stats` / `match:waiting` / `duel:incoming` payloads keep their current app-shell state objects stable and do not replay direct-duel doorbell audio, chat/member/action-hint/action-bar widgets ignore clock-only player time changes where their rendered inputs are unchanged, finished-room close countdown ticks stay local to `RoomHeader`, timed request toasts depend on request-state keys instead of whole-room clock churn, and point buttons stay memoized; startup preload tests guard current-user-accessible assets so visible/owned home, shop, inventory, character, BGM, and voice resources block first entry while timed-out resources retry after entry.


- App audio runtime state now lives in `src/app/useAudioRuntimeState.js`, keeping local audio settings persistence and socket-reconnect resume signaling out of the `src/app/App.jsx` composition root.

- Admin console styles now follow the import-only CSS domain pattern through `src/styles/admin.css` and `src/styles/admin/`, so character, audit, gacha, achievement, and responsive rules no longer live in the root stylesheet entry. `src/styles/admin/polish.css` is the final desktop admin control skin, keeping buttons, inputs, tables, tabs, danger actions, and close controls light, readable, and consistent after feature-specific admin CSS.

- Lobby, house manual, match entry, and watch-list styles now follow the same import-only pattern through `src/styles/lobby.css` and `src/styles/lobby/`, keeping profile, character, match, watch shell, and phone fallback rules out of the root stylesheet entry.

- Bright School modal cleanup now follows the import-only pattern through `src/styles/themes/bright-school/modals.css` and `src/styles/themes/bright-school/modals/`, keeping handbook, settings, selected-action, resume, result, popover, and stage-decoration repairs in focused files.
- Bright School effects now follow the import-only pattern through `src/styles/themes/bright-school/effects.css` and `src/styles/themes/bright-school/effects/`, keeping selected controls, skill action glow/disabled state, board targeting, board marks, keyframes, and reduced-motion rules in focused files.
- Bright School radical purge now follows the import-only pattern through `src/styles/themes/bright-school/radical-purge.css` and `src/styles/themes/bright-school/radical-purge/`, keeping early emergency cleanup rules for home controls, handbook/profile surfaces, character detail, commerce/social rows, and room action controls in focused files.
- Bright School specificity overrides now follow the import-only pattern through `src/styles/themes/bright-school/specificity-overrides.css` and `src/styles/themes/bright-school/specificity-overrides/`, keeping global reset, panel shells, form controls, settings panels, character details, buttons, scrollbars, and addendum notes in focused files.
- Bright School room readability now follows the import-only pattern through `src/styles/themes/bright-school/room.css` and `src/styles/themes/bright-school/room/`, keeping header/exit controls, player status, floating skill panels, player name controls, side tags, board coordinates, and flat control cleanup in focused files.
- Shared room player, timer, capture, skill chip, mobile tap tooltip, and color badge styling now follows the nested import-only pattern through `src/styles/room/players-timers-skills.css` and `src/styles/room/players-timers-skills/`, with style contract coverage guarding the sub-entry import order.
- Bright School narrow-desktop home-stage safety now follows the nested import-only pattern through `src/styles/mobile-adaptive/home-narrow-desktop.css` and `src/styles/mobile-adaptive/home-narrow-desktop/`, separating the common region reset, wide stage, compact stage, micro-stage scroll, and short-height stack breakpoints.
- Bright School gallery polish now follows the import-only pattern through `src/styles/themes/bright-school/gallery-polish.css` and `src/styles/themes/bright-school/gallery-polish/`, keeping theme tokens, home image entries/art, paper surfaces, chat paper grid, interaction states, and addendum notes in focused files.
- Bright School commerce shop polish now follows the nested import-only pattern through `src/styles/themes/bright-school/commerce/shop.css` and `src/styles/themes/bright-school/commerce/shop/`, keeping sidebar/wallet, product grid, item detail dialog, and responsive rules in focused files.
- Bright School commerce warehouse/profile polish now follows the nested import-only pattern through `src/styles/themes/bright-school/commerce/warehouse-profile.css` and `src/styles/themes/bright-school/commerce/warehouse-profile/`, keeping typography/numeric text, profile stats, warehouse header, empty state, item cards, and mobile item cards in focused files.
- Startorch HUD hardening now follows the nested import-only pattern through `src/styles/hud-components/hud-hardening.css` and `src/styles/hud-components/hud-hardening/`, keeping tokens/shell/scrollbars, input/settings/auth contrast, inventory tags, home hologram entries, character deploy/detail styling, shop pagination/owned badges, warehouse surfaces, and friend action rows in focused files.
- Shared mobile battle shell and dock styling now follows the nested import-only pattern through `src/styles/mobile-room/base-shell-dock.css` and `src/styles/mobile-room/base-shell-dock/`, keeping shell/header/menu, flat control reset, viewport/dock shell, player/timer strip, board viewport, dock tabs/actions, and decision/chat panels in focused files.
- Final portrait mobile battle-room safety now follows the nested import-only pattern through `src/styles/mobile-adaptive/mobile-room-portrait.css` and `src/styles/mobile-adaptive/mobile-room-portrait/`, keeping shell/header/menu, neutral chrome reset, viewport shell, player strips, board viewport, dock panels, and action/decision controls in focused files.
- Startorch terminal modal system styling now follows the nested import-only pattern through `src/styles/modals/terminal-system.css` and `src/styles/modals/terminal-system/`, keeping terminal tokens/backdrop, modal chrome, close/header actions, terminal buttons, replay/profile surfaces, result modal layout, and outcome/resume actions in focused files.
- Shared gacha modal styling now follows the nested import-only pattern through `src/styles/commerce/gacha.css` and `src/styles/commerce/gacha/`, keeping modal tabs, featured prize stack, machine stage, featured prize, control panel, list/result dialogs, and animations in focused files.
- Shared recruitment modal styling follows the same nested import-only pattern through `src/styles/commerce/recruitment.css` and `src/styles/commerce/recruitment/`; Bright School has a matching commerce override, the bulletin-board stage uses flat outlined imagegen assets as the board background and paper-card surface while DOM/CSS owns text and layout. Non-result recruitment cards use a CSS watermark span for the active item image instead of foreground item artwork, ready cards remove the envelope sticker and center the single response action, result portraits stay vertically centered, and mobile keeps the third-row item/use controls on one line.
- Shared social/profile/leaderboard styling now follows the nested import-only pattern through `src/styles/commerce/social-profile.css` and `src/styles/commerce/social-profile/`, keeping modal shells, friends toolbar/search, friends list/status, friend actions/notices, duel request banner, leaderboard table, and owned decoration section rules in focused files.
- General CSS architecture guard coverage now lives in `src/styles/styleContract.test.js`: CSS files that contain `@import` must remain import-only, and 6000-byte-or-larger CSS files are blocked unless they are existing known-debt files that do not grow beyond their recorded baseline.
- Bright School portrait mobile house/profile styling now follows the nested import-only pattern through `src/styles/themes/bright-school/mobile/house-profile.css` and `src/styles/themes/bright-school/mobile/house-profile/`, keeping shell/profile stats, character grid/cards, owned decorations, and character detail/music controls in focused files.
- Bright School portrait mobile home shell now follows the same nested import-only pattern through `src/styles/themes/bright-school/mobile/home-shell.css` and `src/styles/themes/bright-school/mobile/home-shell/`, keeping shell base, top strip/menu, main stage, player plaque, and entries/utility/footer rules in focused files.
- Bright School desktop student ID card styling now follows the nested import-only pattern through `src/styles/themes/bright-school/home/student-id-card.css` and `src/styles/themes/bright-school/home/student-id-card/`, keeping card shell/avatar, identity name, user identity tag, and mode stats rules in focused files.
- Bright School portrait mobile commerce and warehouse layout now follows the nested import-only pattern through `src/styles/themes/bright-school/mobile/commerce-warehouse.css` and `src/styles/themes/bright-school/mobile/commerce-warehouse/`, keeping shop layout, warehouse shell/grid, and warehouse item card rules in focused files while removing a duplicated shop mobile block.
- Bright School portrait mobile list/settings/social layout now follows the nested import-only pattern through `src/styles/themes/bright-school/mobile/lists-settings.css` and `src/styles/themes/bright-school/mobile/lists-settings/`, keeping list scroll widths, watch rows, friends rows, table shells, leaderboard cards, replay cards, and toolbar/card-border rules in focused files.
- Bright School final mobile profile/house records now follow the nested import-only pattern through `src/styles/mobile-adaptive/bright-school-overrides/profile-house-records.css` and `src/styles/mobile-adaptive/bright-school-overrides/profile-house-records/`, keeping house/profile stat fixes, profile resume stats, character record dialog rules, and embedded resume character records in focused files.

- Admin-managed character `sortOrder` is persistent data, not a seed-owned builtin field. `seedCharacters` only assigns fallback order when creating missing builtin characters, public `/api/characters` includes `sortOrder`, and the member manual derives its character list through the shared catalog sorter so saved admin order survives server restarts and reaches player UI.

- ChangLi (`changli`) uses the `double-move` active skill. The server persists the public chained-action state in `game.extraTurn`; board confirmation is not tied to an intersection, and restoring during ChangLi skill preview resolves directly into the double-move state without replaying the banner, sound, or animation.
- Lynae (`lynae`) `spray-stone` records its resolved `randomTargetId` in skill history. Replay reconstruction must pass that recorded target back into the shared spray resolver instead of rerolling, so replay step changes cannot turn different historical stones into spray stones.
- Hidden-hand is an active skill history entry with `effectType: "hidden-hand"`, so ChangLi unlock checks treat Aemeath's resolved hidden-hand the same as other opponent active skills while still ignoring passive effects. Aemeath's board presentation now replaces the older data sweep with a 1500ms green Pixi circuit-board takeover from board center to edge, including only line-based circuit lanes, sparse diagonal links, a light outline membrane, and procedural low-startup/high-data/takeover-pulse SFX while avoiding any target-point focus, center green ring, or final full-board darkening pass. Bright School's late skill-action layer also owns a disabled override for `.action-bar .skill-action:disabled`, keeping unavailable skills gray instead of inheriting active/normal gradients.
- Aemeath `hidden-hand` now creates a public `game.derivedSkills[color]` slot for the derived active skill `voyage-star` / “远航星” after the hidden hand resolves. Owner views keep `sourceHiddenHandId`; opponent/spectator views redact that source id through `gameViewForColor` while still showing the derived skill name and remaining use count. `voyage-star` is a no-target board-surface confirmation skill with one derived use, default numeric overclock 5, no turn consumption, and availability gated by the source hidden hand still existing, belonging to the caster, and remaining unexposed. Its shared rules erase the source cross, remove stones around those erased points, clear ko, resolve zero-liberty cleanup as skill removals, persist star-crater invalid points with `skillEffect: "voyage-star-erased-point"`, and keep replay deterministic from history metadata.
- Voyage Star's board presentation stores the source hidden-hand center as `voyage-star-crater-point` and the remaining cross intersections as `voyage-star-erased-point`; only one crater marker renders as a board-level overlay whose `left/top` are derived from the source history id's board coordinates. The marker uses the supplied heart/star artwork converted to `/assets/effects/voyage-star-crater.webp`, is sized to 3.5 board-cell edge lengths, and continuously emits tight pink-purple/cyan drop-shadow glow that follows the WebP alpha shape instead of a round haze. The marker is not a child of the erased `.point`, so its center is independent from point-child layout, stone offsets, and theme point repairs. Pixi board effects resolve point centers from the rendered `.point[data-point-id]` DOM rectangles first, falling back to formula coordinates only when the DOM point is unavailable, so desktop and mobile themes share the same visual coordinate source as stones and grid cells. The Pixi cast preloads the crater WebP but does not draw a temporary crater: it begins with a subtle white-gold omen and vertical beams, drops a white/ice-blue knight greatsword from above the board with a short trail, then keeps the sword visible through impact while a `quakeLayer` applies visual-only shake, concentric earthquake ellipses, and deterministic dust sparks. Its yellow-white explosion cover is rendered above the sword, impact glow, and removal particles, expands from the skill center with a solid warm-white core whose radius is at least 2.2 board cells so the roughly four-cell crater area stays masked, then blends through low-alpha white-to-warm-yellow discs whose outer feather fades to zero alpha. It switches to a fully opaque white full-board cover once the radius reaches the board corners so neither the board nor the resolved crater can show through during the full-cover stage, holds that opaque cover for resolution, then dissolves it with easing plus outward yellow-white rings, radial streaks, and spark fragments instead of hard-cutting from pure white back to the board. The impact glow also fades before full-cover, and the sword is forced fully transparent as soon as that full-board cover is reached. The server resolves `voyage-star` during this full-cover window, and `BoardSkillEffects` keeps the already-started Pixi cast alive after `pendingSkill` clears so the cover can hide the resolved DOM crater and removed stones until the effect ends. This prevents the crater from showing through too early, avoids a sudden post-effect crater pop, and prevents a Pixi crater plus DOM crater double-flash. It does not stack multiple craters or also render Sigrika's erased-field marker. The action bar reads the effective self skill from the current room player snapshot on both desktop and mobile, keeping the button label in sync when Aemeath's derived skill becomes active. Replay reconstruction consumes the `voyage-star` history metadata directly so mobile and desktop replay boards show the erased cross even when the live derived-skill slot is no longer available.
- Board latest-action marking treats Aemeath `hidden-hand` and `voyage-star` as point actions, alongside ordinary moves and flip-stone. This moves the red latest-action ring onto the hidden-hand placement when 小爱出击 resolves and prevents the ring from remaining on the previous ordinary move after 远航星 erases its source point, avoiding false visual evidence that the derived skill was centered elsewhere.
- Bright School board guards keep DOM-only board effect layers transparent and restore their pseudo-elements so broad theme firewall rules such as `[class*="row"]` cannot turn QiuYuan's slash overlay into a blank paper panel or a plain bar over the board; Mornye's protocol marker remains a pointer-events-none point child and never changes board hit testing. The playable board surface is shared through `--board-wood-texture`, currently `/assets/boards/go-board-background-reference-color-vertical-2048.webp`, a low-saturation vertical bamboo/wood WebP asset reused by the base room CSS and Bright School guard layers.
- Board move preview and mobile touch-confirmation markers are point-local visual effects centered by the `.point` grid alignment contract, so ChangLi's extra-turn ordinary placement hints stay on the same intersections as stones and SVG grid lines. Touch confirmation uses only the inner `.touch-confirm-marker`; `.point.touch-confirming::before` stays hidden and mobile confirmation does not scale the whole point button, preventing duplicate blue rings and apparent marker drift.
- Default black/white board stones are ringless: shared stone CSS and Bright School default overrides avoid `border` and `inset` shadow outlines, while semantic skill states can still draw glow/state halos.
- Chisa's `liberty-purge` now has a post-banner targeted Pixi cast: `Board` first shows a delayed pending placement on the target point using that player's selected stone decoration, then `BoardSkillEffects` reads `removalMarkIds` and draws several saturated red scissor slashes in sequence only over the points that will receive removal marks, excluding the newly placed stone. The server derives the board-effect window from `removalMarkIds.length`, so rooms with many removals keep the pending preview open until every removal slash has had time to play; the persistent skill result appears immediately after the slashes fade. A resolved Chisa placement that remains on the board carries the persistent `liberty-purge-stone` eerie red glow instead of reusing the latest-move ring, while resolved `.liberty-purge-removal-mark` crosses remain point-local DOM effects centered with `left/top: 50%` and `translate(-50%, -50%)`.

## Modal Layering Note

- Browser request toasts render through a `document.body` portal, keeping them outside `.mobile-room-screen` so mobile board sizing and position do not shift when draw, counting, or result-confirmation request toasts appear or disappear.
- Chisa (`chisa`) uses the `liberty-purge` active skill. The server resolves a normal legal move first, then removes all one-liberty groups from a board snapshot, records `removalMarkIds`, clamps negative overclock deltas to zero, clears ko, and keeps `.liberty-purge-removal-mark` red crosses until the opponent's next real turn ends. Because `liberty-purge` places a real stone, the placed point records `skillEffect: "liberty-purge-stone"` / `skillEffectOwner` for its persistent red glow when it survives the purge, while `lastMarkedAction` does not add the generic latest-move ring to that skill placement. Admin users own Chisa by default until recruitment is implemented.
- Shared `.modal-backdrop` stacks above room `--room-floating-z` surfaces so request and confirmation modals dim skill chips, chat controls, and room member popovers together. 房间内申请和棋、申请数子与确认数子结果的定时 toast 由流程响应、阶段变化或自动消失关闭，不提供手动关闭按钮。
- Character duplicate chain counts remain in user data for reward/progression logic, but player-facing desktop and mobile UI hides character-chain badges on portrait surfaces.
- Character portrait assets should stay on stable transparent 640x640 canvases when replacing built-in portraits. Nabomo uses `/assets/nabomo.webp`; the current WebP replacement keeps a 640x640 RGBA canvas with an alpha content height matching the existing portrait scale and a file size in the same range as other WebP portraits, while `/assets/nabomo.png` is kept visually in sync for stale catalog/path compatibility.
- Mobile character-detail dialogs keep the character name horizontal and single-line, place the compact BGM preview capsule at the top-right of the heading row, left-align skill and character-description copy, and omit the standalone skill-cost badge; Bright School theme layers and the final `mobile-adaptive` safety layer repeat these constraints so narrow phones cannot fall back to vertical names or centered body text.
- Bright School portrait phone polish that must outlive theme overrides is centralized in the final import-only `src/styles/mobile-adaptive/bright-school-portrait.css` layer and split under `src/styles/mobile-adaptive/bright-school-portrait/`, while final mobile social/warehouse safeguards keep inventory item cards at a stable 88px minimum height for readable item details; the portrait polish includes home player plaque nameplate fitting, centered container-sized plaque mode stats, hidden character-chain portrait badges, the two-row resume header, resume character records that take remaining modal height and scroll internally on short phones, mobile room tabs that change selected background without press-offset motion, and single-line shop coin wallet treatment. Login auth chrome keeps the brand and login/register segmented wrappers transparent on desktop and mobile, while the outer login card retains the Bright School lower-right hard shadow. Desktop and narrow-desktop home image entries also rely on the final `src/styles/mobile-adaptive/home-narrow-desktop.css` safety layer: entry buttons may keep visible labels/shadows outside their frame, but their actual `<img>` art must stay `border-box`, `max-height: 100%`, and `object-fit: contain` so the member manual and match artwork cannot spill out of the bottom of the card.
- Resume character records in the final Bright School mobile layers reuse the detailed user profile character-list rhythm: the outer section stays lightweight, while compact rows carry avatar/name plus five right-aligned stat columns for total games, wins, losses, draws, and win rate. The embedded resume list and user profile details both keep those stat columns on one line with tabular numbers, while separate nested record dialogs use the same aligned column structure inside their scroll region.
- Mobile user profile stat cards center their label and value content inside each card; the record card keeps total games and win/loss/draw as compact centered lines sized to stay within the card.
- User profile footer actions reuse the resume replay button's green treatment for "对局回放" on desktop and mobile; disabled replay and relationship actions that are already effective, such as "已是好友" or "已在黑名单", keep their native disabled semantics and render as shared gray inactive controls in the final Bright School layers.
- Desktop replay-list tables reserve a non-ellipsizing time column for full zh-CN timestamps, while mobile replay lists keep the card-style layout and final Bright School portrait overrides.
- Friend-list action rows and room member username popovers keep unavailable social actions as native `disabled` controls with gray inactive styling across the base, Startorch HUD, and Bright School theme layers; the not-yet-open direct-message entry stays disabled in both surfaces.
- User profile detail cards now include bottom-right icon-only like/report actions inside the portrait/username hero area. Likes show a count and are limited to one like per target per Asia/Shanghai day; self and already-liked states render disabled gray. Reports collect text in a fixed viewport-centered dialog, remain internally scrollable on short viewports, and are protected by the final `mobile-adaptive/profile-report-dialog.css` safety layer so Bright School wide floating-modal rules cannot stretch the input sheet off-screen; submitted reports feed the read-only admin "用户举报" tab with reporter, reported user, time, and content.
- Mobile leaderboard rows use a compact card grid with no extra left list inset, smaller rank badges, larger character portraits, and a centered username/rank stack so the rank label shares the same vertical center axis as the username/nameplate. Compact leaderboard `UserIdentity` names provide a fit font-size token so legacy/generated usernames shrink before they would fall back to clipped ellipses. Leaderboard rows representing the current user, including the pinned "my ranking" row, use a light green surface on desktop and mobile so they remain visually distinct from ordinary leaderboard entries.
- Leaderboard top-three emphasis belongs on the whole row, not the rank-number badge: rank 1 uses `/assets/leaderboard/top-rank-gold.webp`, rank 2 uses `/assets/leaderboard/top-rank-silver.webp`, and rank 3 uses `/assets/leaderboard/top-rank-bronze.webp` as subtle imagegen satin-paper textures with a light readability overlay on desktop leaderboard rows. The top-three rank number itself stays a plain readable number with no triangular badge background. Desktop leaderboard portraits render through a dedicated `leaderboard-avatar` frame with character id metadata so full-body portrait thumbnails can be positioned inside the avatar slot without shifting the text columns or mobile card layout.
- Selected tab-like controls and selected option buttons share a pressed visual contract across desktop and mobile: active/aria-selected/aria-pressed states shorten the drop shadow, shift down slightly, and add an inset shadow so the selected control reads as physically pressed rather than merely highlighted.
- Achievement and personalization modal headers keep their close button in the same grid row as the title on desktop and mobile; the final Bright School mobile layer repeats this contract so global absolute close-button rules cannot push nested modal close controls onto the divider line.
- Room player info panels use `.active-turn` as the current-turn visual contract: the player whose turn it is turns yellow on both desktop and mobile, and late theme layers such as Bright School must preserve that yellow turn-state cue.
- Room player disconnect state is shown through the portrait background, not a text badge: active rooms add `.disconnected-portrait` to the disconnected player's portrait wrapper and render a light red portrait background on desktop and mobile, while finished rooms suppress the disconnect state.
- Desktop resume headers reserve separate right-aligned grid columns for the visible coin wallet and close button, preventing the global absolute close-button rule from colliding with currency capsules.
- Mobile resume headers use a final high-specificity Bright School override so the close button stays in the first-row right slot and the visible coin wallet stays inside a shrinkable second-row right slot without overflowing past the modal edge.
- Desktop resume modals keep the outer window fixed inside the viewport with hidden outer overflow; embedded character records own the vertical scroll region, while the `.profile-grid.top-stats-bar` stats wrapper stays overflow-visible so rating/rank help tooltips can escape the stat cards without being clipped. Desktop user profile detail modals likewise give the character-record list a bounded scroll area, and character detail headers reserve right-side space so the compact BGM player cannot collide with the close button.
- Resume and user profile recent-result rows render a real `.recent-result-label` reading "最近十盘的战绩" above the marker chips inside `.profile-rank-results`; the label is small black text rather than a background watermark, and the chips/empty state remain the primary row content on desktop and mobile.
- Personalization sections now expose one "style picker" entry per equipment category. Each picker opens a nested modal containing default plus unlocked decorations, closes immediately after selection, and updates the local try-on preview while the existing save action remains the only persistence step.

## Music Management Note

- 莫宁（`mornye`）已接入完整角色语音：技能、出战、对局开始、读秒开始、剩余读秒次数、10 到 1 秒倒计时、胜负和结果，以及 `house-detail` 角色详情语音。部员手册中打开莫宁角色详情或点击详情内的“描述”文本区域会通过现有 `house-detail` 事件播放 `/assets/voice/mornye_detail.ogg`；关闭角色详情弹窗或关闭整个部员手册时会调用 `stopVoicePlayback()` 停止当前角色语音，避免详情长语音在窗口关闭后继续播放。
- 角色技能 BGM 当前配置：达妮娅使用 canonical `denia-skill-default`（`bgm_*`），西格莉卡使用 `sigrika_intro_once.ogg` + `sigrika_loop.ogg`，爱弥斯“小爱出击”使用 `aemeath0_once.ogg` + `aemeath0_loop.ogg`，爱弥斯“远航星”使用 `lhl_intro_once.ogg` + `lhl_loop.ogg`，猪小仙使用 `matoya_*`，娜波摩使用 `busizhe_*`，千咲使用 `chisa_intro_once.ogg` + `chisa_loop.ogg`，长离使用 `changli_intro_once.ogg` + `changli_loop.ogg`，莫宁使用 `mornye_intro_once.ogg` + `mornye_loop.ogg`。旧达妮娅 slug 会在启动清理阶段迁移/删除，运行时 BGM resolver 只按 canonical `denia` 查找达妮娅技能 BGM；派生技能的 `musicTrackId` 会从 pending skill 延续到最新历史技能解析，避免演出中途回退到角色默认技能 BGM。未来爱弥斯商城技能 BGM 若替换“小爱出击”默认曲，可继续使用角色级 `musicSelections.skill.aemeath`；若替换“远航星”等派生技默认曲，则应新增按 `effectType` 作用域的选择键并在解析时优先于角色级选择。

- Settings > Audio persists optional per-channel mute flags under `audioSettings.muted`; clicking an audio row title toggles mute without changing the slider percentage, and moving the slider clears that channel's mute flag.
- Settings > Interface presents three player-facing theme choices in a three-column grid on desktop and mobile. Theme option labels are the direct style names without a repeated `主题：` prefix or explanatory parenthetical. Only the first option maps to the current Bright School theme; the two future theme buttons keep native button feedback and show the non-blocking `敬请期待~` toast without changing persisted theme state. On portrait mobile, the Interface tab gets its own taller `settings-tab-theme` modal state so the three theme buttons fit without an inner vertical scrollbar.

- Character detail BGM preview now gives immediate loading feedback before first-use WebAudio decode finishes, keeps the background BGM pause request scoped to successful preview startup, and presents the Bright School control as a unified campus-notebook hand-drawn player where Rough.js owns the visible outer frame/play-button ring, CSS provides paper texture/sticker state fills/prominent track labeling, and desktop/mobile size contracts stay separate.
- 后台“音乐管理”只维护 `MUSIC_TRACKS` 静态曲目的显示名覆盖值；`MusicTrackSetting` 不改变轨道 id、类型、角色绑定或音频文件，玩家侧 `/api/music-tracks`、角色 BGM 选择、商城音乐展示和抽卡音乐奖项显示会使用合并后的曲目名称。
- 仇远（`qiuyuan`）的默认技能 BGM 是 `qiuyuan-skill-default`，使用两段式 `intro-loop` 播放：`/assets/music/qiuyuan_intro_once.ogg` 播放一次后接 `/assets/music/qiuyuan_loop.ogg` 循环；第二版技能 BGM `qiuyuan-skill-zhouwo` 显示名为“肘我”，使用单文件循环 `/assets/music/qiuyuan_zhouwo_loop.ogg`，商城商品图使用居中裁切 WebP `/assets/items/qiuyuan-zhouwo.webp`，不默认解锁但可作为商城音乐商品购买；启动内置商城 seed 会为已存在且 `imageUrl` 为空的 `qiuyuan-skill-zhouwo` 音乐商品回填该商品图，不覆盖后台已配置的非空图片，购买后会出现在仇远角色详情播放器的技能 BGM 选择中。
- 仇远（`qiuyuan`）的技能发动语音配置为两个候选音频：`/assets/voice/qiuyuan_skill_cast.ogg` 和 `/assets/voice/qiuyuan_skill_cast_1.ogg`；`skill-cast` 系统语音每次解析时随机选择一个，启动/对局预加载会展开全部候选路径。
- 爱弥斯（`aemeath`）普通技能语音继续使用 `/assets/voice/aemeath_skill_cast.ogg`；远航星（`voyage-star`）技能横幅会通过 `skill-cast:voyage-star` 优先播放 `/assets/voice/aemeath_skill_cast_voyage.ogg`。
- 琳奈（`lynae`）已接入角色技能语音 `/assets/voice/lynae_skill_cast.ogg`，并配置出战、对局开始、读秒开始/剩余次数、10 到 1 秒倒计时和胜负和结果系统语音。
- 长离（`changli`）已接入角色技能语音 `/assets/voice/changli_skill_cast.ogg`，并配置出战、普通对局开始、读秒开始/剩余次数、10 到 1 秒倒计时和胜负和结果系统语音；五子棋模式开局会优先播放 `/assets/voice/changli_wuzi_match_start.ogg`。
- 千咲（`chisa`）已接入角色技能语音 `/assets/voice/chisa_skill_cast.ogg`，并配置出战、对局开始、读秒开始/剩余次数、10 到 1 秒倒计时和胜负和结果系统语音。
- 琳奈（`lynae`）的默认技能 BGM 是 `lynae-skill-default`，使用两段式 `intro-loop` 播放：`/assets/music/lynae_intro_once.ogg` 播放一次后接 `/assets/music/lynae_loop.ogg` 循环；该曲目默认解锁并跟随角色 BGM 选择/技能预览解析逻辑。

- 千咲（`chisa`）的默认技能 BGM 是 `chisa-skill-default`，使用两段式 `intro-loop` 播放：`/assets/music/chisa_intro_once.ogg` 播放一次后接 `/assets/music/chisa_loop.ogg` 循环；该曲目默认解锁并跟随角色 BGM 选择/技能预览解析逻辑。

- 长离（`changli`）的默认技能 BGM 是 `changli-skill-default`，使用两段式 `intro-loop` 播放：`/assets/music/changli_intro_once.ogg` 播放一次后接 `/assets/music/changli_loop.ogg` 循环；该曲目默认解锁并跟随角色 BGM 选择/技能预览解析逻辑。

- 莫宁（`mornye`）的默认技能 BGM 是 `mornye-skill-default`，使用两段式 `intro-loop` 播放：`/assets/music/mornye_intro_once.ogg` 播放一次后接 `/assets/music/mornye_loop.ogg` 循环；该曲目默认解锁并跟随角色 BGM 选择/技能预览解析逻辑。

## Dependency Notes

- `roughjs` is installed as a runtime drawing dependency for Bright School hand-drawn SVG/Canvas decoration, such as notebook outlines, stickers, circles, and annotation strokes. Character detail BGM previews import it for the stable campus notebook sketch layer.
- Tailwind CSS v4 is installed as a build-time utility CSS dependency through `@tailwindcss/vite`. The app imports `src/styles/tailwind.css` before the final theme layer; that entry imports only Tailwind `theme.css` and `utilities.css` with the `tw:` prefix and omits preflight/global resets so existing Bright School CSS contracts are not reset.
- `npm test` excludes local Codex scratch folders such as `.codex-run/` in addition to worktrees, e2e tests, and stability tests, so temporary staged snapshots cannot be collected as Vitest suites during handoff.
- Player theme metadata now has one source of truth in `src/app/visualTheme.js`: `VISUAL_THEME_OPTIONS` includes both available and future settings choices, while `VISUAL_THEMES` / `VISUAL_THEME_IDS` expose only enabled themes that can be persisted and imported. Settings renders from the full option registry, so future styles can be listed without duplicating labels in the modal. CSS refactors must preserve the root theme order `shared -> isolation -> theme-components -> bright-school -> mobile-adaptive`; board, Pixi skill overlays, point buttons, and Bright School effect files remain protected skill-presentation surfaces and should not be broad-selector cleanup targets.
- CSS round-2 inventory lives in `src/styles/cssLayerInventory.js` with tests in `src/styles/cssLayerInventory.test.js`. The inventory separates reorganizable shared domains from high-risk gameplay/room CSS, Bright School-specific overrides, final mobile safety layers, and skill-presentation protected files so later cleanup can stay scoped before visual and skill regression verification.
- CSS round-3 and round-4 follow-ups are also encoded in `src/styles/cssLayerInventory.js`: `CSS_ROUND3_SHARED_SPLITS` records import-only shared-domain splits for `base/home-legacy-grid.css`, `base/home-stage-artboard.css`, `hud-components/pop-tech-terminal.css`, `mobile-modals/phone-house-resume.css`, `modals/character-opening.css`, `responsive/phone-portrait-room.css`, `modals/mailbox.css`, and `commerce/recruitment/board.css`; `CSS_ROUND4_REGRESSION_CHECKS` records the required static CSS, focused board/skill unit, and desktop/mobile Pixi visual stability gates. `npm run verify:battle-fixes` now includes CSS inventory/theme contracts plus BoardSkillEffects, BoardAmbientEffects, skill registry, skill SFX scheduling, and mobile point-confirmation tests.
- Final mobile safety splits are tracked separately as `CSS_FINAL_MOBILE_SAFETY_SPLITS` because `mobile-adaptive.css` remains the post-theme override layer. `mobile-adaptive/mobile-profile-records.css` now delegates to `mobile-adaptive/mobile-profile-records/` while preserving the 768px profile/resume record cascade, `mobile-adaptive/phone-core.css` delegates to `mobile-adaptive/phone-core/` for match mode, shell controls, modal tabs, and scroll/detail/result safety, `mobile-adaptive/phone-gacha.css` delegates to `mobile-adaptive/phone-gacha/` for legacy gacha modal, stage, controls, and result dialog survival rules, Bright School final leaderboard cards delegate to `mobile-adaptive/bright-school-overrides/leaderboard-cards/`, and Bright School portrait resume modal layout/settings tabs delegate to `mobile-adaptive/bright-school-portrait/resume-modal-layout/` and `mobile-adaptive/bright-school-portrait/settings-tabs/`.
- Theme overlay splits are tracked separately as `CSS_THEME_OVERLAY_SPLITS`. `themes/shared.css` now delegates to `themes/shared/` for player theme tokens, theme settings controls, and player-surface wiring, `themes/theme-components.css` delegates to `themes/theme-components/` for outcome badges, skill chips, and replay row semantic colors, Bright School component repairs such as `foundation-home.css`, `warehouse-character.css`, and `notebook-polish.css` delegate to matching nested directories, and Bright School portrait room `shell-header-menu.css`, `viewport-player-strips.css`, and `dock-actions.css` now delegate to matching directories under `themes/bright-school/mobile/room/` while preserving their positions before later Bright School repairs and final mobile safety layers.
- Gameplay room splits are tracked separately as `CSS_GAMEPLAY_ROOM_SPLITS` because these selectors affect live room controls. `room/actions-requests.css` now delegates to `room/actions-requests/` for room toggles/action bars, decision/scoring panels, timed request toasts, test tools, replay controls, and disabled action states, `mobile-room/portrait-room.css` delegates to `mobile-room/portrait-room/` for shared portrait viewport, player strip, board/dock, decision, and action-hint rules, and `room-terminal/players-timers-skills.css` delegates to `room-terminal/players-timers-skills/` for the Startorch battlefield player panel, timer, captures, skill chip, detail panel, and sheen keyframe skin while preserving their current import positions before later responsive and theme repair layers.

## Achievement And Personalization Note

- 用户名注册/搜索校验允许中文、日文、韩文、半角英文、数字和下划线；按显示宽度限制为最多 8 个半角字符宽度，等价于最多 4 个中日韩字符或 8 个半角字符。注册页把用户名、密码和确认密码规范放在输入框 placeholder 中，用户开始输入后自然隐藏；失焦或提交时才在对应字段下方显示错误。 Legacy startup cleanup trims overlong existing usernames to the trailing 8 display-width units and adds a short numeric marker only when needed to preserve username uniqueness.
- 成就系统以 `Achievement`、`AchievementRewardAsset`、`UserAchievement`、`AchievementCounter` 和 `UserAchievementEquipment` 为核心模型；启动时 `ensureAchievementSchema` 会先于角色/商店种子任务运行，为旧 SQLite 自动补表，并为 `Character`、`Decoration`、`ShopItem` 补 `source` 字段。玩家侧 `/api/achievements` 返回合并后的成就列表与本次新解锁成就，`/api/me/achievement-equipment` 读写称号、徽章和用户名背景装备；成就目标本体由代码/种子维护，后台 `/api/admin/achievements` 只允许修改成就名、成就内容、奖励资产和排序，`/api/admin/achievement-reward-assets` 继续管理奖励资产并写审计日志。
- `/api/me`、`GET/PATCH /api/me/achievement-equipment` 会同时返回 `achievementEquipment` 装备 id 与 `achievementEquipmentAssets` / `equipmentAssets` 奖励资产显示数据；socket 登录用户、排行榜和社交用户列表/资料也会批量附加 `achievementEquipmentAssets`。前端通过共享 `UserIdentity` 在对弈玩家名、房间成员、排行榜、好友/黑名单、资料卡、观战列表和结算赢家名等完整用户展示点渲染称号、徽章和用户名背景；好友/黑名单列表行只展示状态、常用角色、用户名和操作入口，不再展示段位或积分，详细资料仍保留完整统计。
- UserIdentity nameplates are rendered as fixed-ratio username badges: users without a nameplate keep natural-width text, while equipped nameplates use a `96px x 25.6px` base slot at `3.75:1`. The background image and horizontal safe padding scale together through scene-owned `--user-nameplate-scale`; username text uses shared `--user-nameplate-font-size: calc(15px * var(--user-nameplate-scale))` so it scales with the fixed badge while remaining independent from username length, and never emits `--user-identity-fit-font-size`. The fallback for extreme legacy names is ellipsis rather than per-name font shrinking. The nameplate background wraps only the username, stays centered, and must not return to parent-width stretching or left/center/right DOM slicing. The existing `/assets/achievements/semantic-nameplate.png` asset was alpha-bound checked and resampled to `900 x 240`; future nameplate PNGs should be alpha-trimmed with `scripts/pngTrim.mjs` and delivered or resampled at the same `3.75:1` ratio.
- 内置成就“你给我吃了什么！？”由 `seedBuiltinAchievements` 在启动时按需创建，监听新增后的 `denia-rainbow-bean-candy` 道具使用事件；玩家请达妮娅吃彩虹豆豆跳跳糖后解锁并获得 100 金币，不回溯统计成就上线前发生过的使用记录。
- 新增内置成就“点亮语义！”使用 `mode_character_wins` 条件统计玩家使用西格莉卡在 `spark`（星炬对弈）模式的胜场，达到 100 胜后解锁 `/assets/achievements/semantic-nameplate.png` 用户名背景；`seedBuiltinAchievements` 会让管理员默认达成所有内置成就并标记奖励已发放。
- 启动初始化会在角色 seed 前运行 `cleanupLegacyDeniaCharacterData`：旧达妮娅 slug `danea`/`denea` 的用户选角和拥有权会迁移到 canonical `denia`，旧角色行会删除，引用旧 slug 的对局记录会删除，角色商品/抽卡/成就奖励目标会改写为 `denia`；公共角色列表会防御性忽略旧 slug，避免旧达妮娅再次出现在前台。
- Runtime stability note: room runtime uses lightweight `room:clock` / `room:patch` and full `room:update` paths, user/socket room indexes for resume and disconnect lookup, throttled persistence for chat/presence patches, forced persistence for key lifecycle updates, and room-code-specific persistence flush before close cleanup deletes a persisted room row so delayed upserts cannot recreate closed rooms.
- Development room test tools are non-production only. In development builds the room action area shows the test-tool group on desktop and mobile; `test-enter-byo-yomi` forces both room players into byo-yomi so countdown voice, timer UI, and clock recovery can be tested quickly. Production still rejects debug test actions even if legacy `ENABLE_TEST_ACTIONS` is set.

## Mailbox System Summary

- Mailbox is an authenticated player/admin HTTP feature. Players open it from the home top-right action area or the mobile menu, read admin-only text mail, manually claim one optional attachment, and can keep at most 20 messages.
- Admins send mailbox batches from the admin console to one user, all current users, or all current plus future users. Item and coin claims update the existing inventory and progress systems.
- Player deletes are soft deletes on `MailboxMessage.deletedAt`; future-eligible global batches still see that delivery history, so a deleted mail is not recreated on the next mailbox summary or list refresh.
- The player mailbox keeps the same list/detail layout for empty inboxes; the left mail list shows the empty state while the detail pane remains reserved. Desktop uses a left-list/right-detail split, while mobile places a horizontally scrollable mail list above the detail pane so newer mail appears on the left and older mail continues to the right. Its player modal title omits the former English subtitle, and the close button stays in the title row on desktop and in the final Bright School portrait mobile safety layer. Mail rows are colored by status: pink for unread/unclaimed, light green for read/unclaimed, and gray for read/completed. The mail detail pane stretches to the bottom of the mailbox content area with modal padding preserved; long mail text scrolls inside the body region while time, title, attachment, and actions stay in the detail frame. The detail pane uses `/assets/mailbox/mail-body-paper.png` as a covered paper background on desktop and mobile, with a light translucent frosted overlay above the art that fades at the edges to protect text readability without hiding the paper, a thicker paper border, and a small lower-right shadow; delete is a small 24px icon-only pink circular trash button in the detail pane's transparent top-right time row.
- App-level overlays must keep `useOverlayState`, `App`, `useAppActions`, and `useOverlayActions` wiring in sync. `closeAllOverlays()` is reused by `match:found`, room resume, and replay entry flows, so a missing overlay setter can break matchmaking before the match-success/preload state is recorded.

## Stability Boundary Update

- Startup preload / room resume race: `useStartupPreload()` now uses `shouldShowStartupPreload({ room: roomRef.current, matchSuccess: matchSuccessRef.current })` before setting `view="preloading"` after `/api/me`; a fast mobile `room:resume` that already restored an active room or pending match cannot be visually covered by the generic login preload route.
- 对局刷新/重连稳定性边界：`room:preload-ready` 是服务端 ack 的幂等协议，`BattleAssetPreloadScreen` 在未收到 `{ ok: true }` 前会退避重试并在 socket `connect` 后立即重发；`useRoomMemory` 会同时记住 active room 和 `match-preloading` pending room，`room:resume` 恢复到 `preloading` 玩家房间时重建 `matchSuccess` 并回到 `match-preloading`，从而重新加载本地资源并再次上报 ready。`connectGameSocket()` 先用 `autoConnect: false` 创建 Socket.IO 客户端，安装 `connect`/`room:resume` 等处理器后再主动连接，并在连接后立即 queue 一次幂等 `room:resume`，避免生产同源快速连接或移动端 polling/websocket 时序让刷新恢复只依赖单个 `connect` 回调。前端所有 full room snapshot 在进入 UI 前通过 `normalizeRoomSnapshot()` 补齐 `players`、`spectators`、`chat`、`game.points/history/captures/skillUses` 等安全默认值；`AppErrorBoundary` 包裹应用根，`view="room"` 但缺少 `room` 或 `user` 时显示恢复加载页而不是空白页面。
- Finished-room 恢复边界：`sigrika-last-room-code` 只保存未结束的玩家房间；玩家关闭有效结果或从 finished 房间退出时会持久写入 `sigrika-dismissed-result-room-code` 并清除 last-room。服务端 `room:resume` 对空 roomCode 不再自动拉起 finished room，只有显式 roomCode 才允许恢复历史结果，因此玩家离开结果页后刷新不会在房间回收前再次弹出同一结果。
- 技能演出与音频恢复边界：`BoardSkillEffects` 继续把 Pixi 作为 presentation-only 覆盖层，严格 CSP 环境下先加载 `pixi.js/unsafe-eval` 再加载 `pixi.js`；真实 `.board-effects-canvas` 是技能演出验收条件，不再渲染 DOM/CSS 替代动画。Pixi app 初始化成功后立即挂载透明 canvas，renderer 资源加载作为独立受监控预热，不能阻塞 canvas 挂载；Pixi 初始化、WebGL、贴图、renderer 或 ticker 异常只会标记 `data-effect-failed` / `data-effect-error`、清理 sound timers 和 canvas，并保持对局页面不白屏，准生产测试仍判该技能失败。BGM 加载会校验 HTTP `response.ok`、记录 decode/fetch 失败源，WebAudio 调度失败时使用 `Audio` fallback，并在 `room:update` 音频基线、socket 重连、`pageshow`、可见性恢复和首次交互后恢复当前曲目。
- 生产缓存边界：`server/staticAssets.js` 只对 Vite hash 产物设置一年 immutable；`public/assets/**` 下的运行时图片、WebP、SVG、音频和技能资源保留短缓存/ETag，云端同名替换资源时不会被旧 immutable 缓存长期锁住。
- 本地准生产稳定性验证入口是 `npm run verify:stability`：脚本先执行 Vite production build，再通过 `playwright.stability.config.js` 启动 `scripts/start-stability-server.mjs`，以 `LOCAL_PROD_STATIC=1` 让同一个 Express/Socket.IO 服务托管 `dist/` 并保留非 production 的本地测试能力；默认端口是 `4173`，可用 `STABILITY_PORT` 或 `PORT` 覆盖。稳定性服务器使用 `NODE_ENV=stability`，仅在该环境把 `/api/auth` 限流上限提高到足够覆盖桌面和移动两组注册/刷新流量，生产环境仍保持严格 auth 限流。稳定性用例覆盖生产包缓存边界、运行时资源失败不白屏、桌面/移动浏览器的对局刷新恢复、星炬与爱弥斯 `hidden-hand` / `voyage-star` 真实 Pixi canvas、finished 房间退出后刷新不重弹结果、以及对手重连不触发本机“操作频繁”toast；它减少云服务器反复部署验证，但最终上线仍需确认 Nginx/CDN 没有覆盖 Node 返回的缓存头。
- App-level overlays are registered in `src/app/overlayRegistry.js`. `useOverlayState`, `modalDismissal`, `useOverlayActions`, and `App.jsx` derive overlay state, close-all behavior, and topmost-modal dismissal from that registry so new overlays do not need separate manual wiring that can break matchmaking or room recovery.
- Mailbox summary polling lives in `src/app/useMailboxSummary.js`, and recruitment ready-state polling/timing lives in `src/app/useRecruitmentReadyState.js`, keeping the app composition root focused on wiring rather than feature-specific polling loops.
- Room broadcast persistence policy is declared in `server/roomBroadcasts.js`: full updates and default patches force persistence, while clock and presence-style patch categories remain throttled/lightweight.
- Finished valid rooms do not close until `server/roomResultPersistence.js` has saved the result record; `server/roomCloseLifecycle.js` retries failed result saves before emitting `room:closed` or deleting persisted room state.
- Gameplay actions pass through the phase matrix in `server/roomActionPhaseGuards.js` before reaching move/pass/resign/skill resolution, so opening, skill preview, dead-stone marking, result review, and finished phases cannot be overwritten by generic board actions.
- Client patch-gap recovery is debounced in `src/app/socketHandlers.js`: duplicate gapped `room:patch` payloads for the same room/revision emit at most one `room:resume` per debounce window, room-code-level in-flight/cooldown state suppresses resume storms during presence reconnects, and full `room:update` / `room:resume` snapshots clear that debounce state. `server/socketGuards.js` separates recovery events (`room:resume`, `room:preload-ready`) from user actions, so excessive recovery traffic can be acked/rejected without showing the player-facing “操作频繁” toast that still protects `game:action` and chat.
- Legacy-to-structured asset sync only deletes structured rows whose `source` is `legacy`; feature-owned rows from achievements, gacha, mailbox, recruitment, or future flows stay owned by their source-specific paths and are merged for public projection.

# 队际赛实现与验证

已实现三人选人及排序记忆、独立匹配、三轮接力、保密立绘、五秒对决演出及己方当轮出战语音、断线恢复、终局完整阵容和独立棋谱回放。首版不写段位、金币或原有个人与角色战绩。

## 验证通过

- 房间集成：真实落子跨过第 40 / 90 手，同步换人、五秒停表、演出期间拒绝操作，棋盘保持。
- 服务端契约：所有权及可用性校验、候场重新校验、取消异步入队、独立队列、观战和对手阵容投影、连下延迟、技能重置、禁先重置、超频保留、快照恢复、终局保存及重试、按阶段回放。
- 组件：阵容排序与记忆、失效角色过滤、角色不足提示；逐轮己方语音去重，观战/回放静音，重连不重播。
- 最近收尾定向回归：6 个测试文件、114 项通过；另外的样式、队际赛契约和 GNU Go 引擎定向回归 5 个文件、131 项通过（两次存在重叠，不合并计数）。
- `npx playwright test --config tests/e2e/team-match.config.js`：3 项通过，1280×900、390×844、360×640。已查看截图，修正主题覆盖灰度和立绘容器塌陷；检查三格等宽、高度、灰度、问号及页面无横向溢出。
- ESLint、生产构建、立绘资源检查、后台默认快照检查、构建后 CSS 契约、生产配置静态检查、系统设计 HTML 生成与一致性检查通过。

## 全量检查剩余问题

最近一次 `npm run check` 在单元测试阶段停下：2646 项通过、5 项失败。GNU Go 子进程测试为 1000ms 超时，定向重跑已通过。以下 4 项原有界面断言仍未通过；本任务未修改它们断言的布局或颜色规则：

- `src/modals/HouseModal.test.js`：履历中成就、钱包、关闭按钮的 DOM 顺序。
- `src/modals/HouseModal.test.js`：部员页移动端背景色字面量。
- `src/modals/ShopModal.test.js`：商店移动端宽度字面量。
- `src/room/RoomScreen.test.js`：桌面对局操作栏网格字面量。

没有把全量检查报告为通过，也没有为消除这些失败而改动其他任务的界面。全量命令未继续执行的资源、构建与配置门禁已单独执行通过。

## 工作区

保留原有未提交修改，未提交代码。浏览器验证使用构建后的独立组件入口；开发服务曾出现请求无响应，因此没有将其等待超时解释为玩法失败。专用浏览器配置提供可复跑的验证路径，无需修改本地数据库。

## 第二轮修订验证

- 阈值改为 40 / 80；Round 2 为 41–80 手，Round 3 从 81 手开始。
- 桌面大立绘按高度放大并居中、左右裁切，采用略斜分隔；候场灰度与问号规则保持。
- 移除左右排序按钮与独立移除按钮，只按角色卡片点击顺序选择；取消后再选排到末位。选中卡片浅绿色、无边框或外投影、按下状态，右上角为细黑边淡黄底深黑字实心圆序号；减少动态效果时禁用位移。
- BGM 回归覆盖 Round 1→2→3、重新连接、新技能切歌与终局停播。
- 7 个定向测试文件共 185 项通过；桌面及两种竖屏浏览器用例 3 项通过，断言选中背景色、边框、投影、按下变换、徽标圆角、桌面大图宽度及垂直居中。
- ESLint、构建和构建 CSS 检查通过。前述其他界面全量失败未在本轮扩展修改。

- 本轮 BGM 修复与对应回归测试、系统设计摘要及生成 HTML 已独立提交：`7983f4a5`。其余队际赛与原有任务改动继续保留在工作区，未混入该提交。

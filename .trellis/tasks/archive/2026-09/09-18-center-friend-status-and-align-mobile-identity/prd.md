# Friend status and portrait alignment

Center status-label text on both axes. On portrait phones, mirror the compact desktop hierarchy: stable status column, portrait, left-aligned nearby username, trailing action. Preserve full-width mobile window, action hit area, nameplate effects, scroll and shadow gutters. Validate status variants and long names at desktop and narrow portrait sizes.

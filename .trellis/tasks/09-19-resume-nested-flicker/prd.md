# 修复履历打开子窗口时闪隐

用户反馈点击成就、个性化等按钮时原窗口先消失，待新窗口加载后一起出现。AppOverlays 的多个懒加载窗口目前共用 Suspense fallback=null，任何新窗口挂起都会隐藏已打开的兄弟窗口。

每个懒加载窗口使用独立 Suspense 边界，保留现有状态、回调、层级和外观。回归测试必须模拟首次模块加载延迟，验证履历始终可见、节点与输入状态保留、关闭子窗口不关闭履历。仅处理窗口加载隔离，不改书签视觉。

# 写作技能选择

- `story-sense` 用于判断上一版问题究竟落在人物、场景积累、对白还是结尾，不直接代写。
- `dialogue` 用于检查同声、直说主题、信息宣讲和均匀对答。
- `human-writing` 用于执行中文虚构重写，要求每场戏有行动或变化，动作已经传达感情时不再解释，并在初稿完成后清除模板句式。
- `documents` 用于保留 Word 格式并完成逐页渲染检查。

本轮不使用长篇连载型小说技能。现稿是互动剧情审阅文档，需要精确保留分支结构与既有格式，不适合按章节长度、章末钩子或连载节奏重构。
